# Changelog

이 파일은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르며, 버전 번호는 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

## [Unreleased]

### Fixed

- **`grid` 의 스팬 셀이 자기가 덮는 트랙만, 모자란 만큼만 키운다.** 스팬만 덮은 트랙이 `fallback_width` — *"그리드 전체에서 가장 넓은 셀"* — 를 받고 있었다. 근거는 *"행렬 폼이 빈 트랙에 주는 답과 같게"* 였는데 그 유비가 틀렸다: 행렬 폼의 빈 트랙에는 **아무것도 안 그려지고**, 이쪽 트랙에는 자식이 지나간다. 자연폭 1612px 헤더가 2열에 걸치면 **2424px**, 3000px 차트가 3열에 걸치면 **9024px**(자기 폭의 세 배)이 됐다.

  이제 각 스팬이 부족분을 자기 트랙들에 **작은 것부터 채워 올리며** 배분한다. `CSS Grid` 의 "distribute extra space across spanned tracks" 를 닮았지만 **포팅은 아니다** — 스펙은 growth limit 에서 트랙을 freeze 하는데 이 구현에는 그 개념이 없다. 빌려온 것은 여기서 중요한 부분, 즉 **균등 분배가 아니라 작은 트랙부터** 라는 쪽이다.

  **처음에는 균등 분배로 썼고 그것이 실제 버그였다.** 먼저 처리된 스팬이 채워 놓은 값이 다음 스팬의 계산에 들어가서, 같은 셀 목록을 **순서만 바꾸면** 캔버스 폭이 132px 달라졌다. 리뷰가 잡았다. water-filling 으로 바꾸니 무작위 400 배치 중 15 가 남았고 — 정렬 키가 `length` 하나뿐이라 **동점이 호출자 순서로 남았기** 때문이다. 동점이야말로 두 스팬이 같은 트랙을 두고 다투는 자리다. 키를 `(length, start, needed)` 로 바꿔 **400/400 순서 무관**이 됐다.

  `_grow` 의 반복에는 상한을 둔다. 각 회차가 남은 양을 소진하거나 가장 낮은 트랙들을 다음 단계로 합치므로 `len(covered)` 회면 충분하고, 상한이 없으면 한 걸음이 float 을 못 움직일 만큼 작아질 때 **끝나지 않는다** — 정렬을 건드린 변이가 실제로 그 상태를 만들었다. 아무것도 닿지 않는 트랙(`ncols=` 가 만들 수 있다)은 fallback 을 유지한다.

- **눈금 밀도가 데이터의 단위에 좌우되지 않는다.** `(0, 3.5)` 는 눈금 8개, `(0, 35)` 는 4개였다 — 같은 모양의 데이터, 단위만 다른데 축이 갈렸다. `3.5 / 5` 는 `0.7` 이고 그 residual `0.7 / 0.1` 은 `6.999999999999999` 이라 `< 7` 분기를 타는 반면, `35 / 5` 는 `7.0` 이라 그러지 않는다. 사다리에 태우기 전에 유효숫자 열 자리로 반올림한다.

  **근거를 한 번 잘못 적었다.** 처음에는 *"수학은 10 이라고 한다"* 고 썼는데 그렇지 않다 — `0.7` 이라고 쓴 double 은 십진 0.7 보다 **작아서**, `Decimal` 로 재든 `Fraction` 으로 재든 `10 ** -exponent` 를 곱하든 정확한 몫은 `6.99999999999999916...` 로 7 미만이다. `span / count` 로 도착한 값에서 *"사용자는 0.7 을 의도했다"* 를 복원하는 산술은 없다. 요구사항은 십진 의도의 복원이 아니라 **단위 불변성**이다.

  그리고 그것은 **대가가 있다.** 분기점에서 `1e-10` 이내인 residual 은 그 값에 나눗셈 오차로 도달했든 원래 그 값이든 위쪽 분기를 탄다 — `_nice_step(6.999999999999999)` 은 5 대신 10 을 낸다. 둘은 같은 비트패턴이라 어떤 반올림도 구별하지 못한다. 방향은 일관성 쪽이다: 호출자에게 `6.999999999999999` 에서 끝나는 도메인은 7 에서 끝나는 도메인이고, 이제 7 의 축을 받는다. 전에는 `0.7`·`6.999999999999999`·`7.0`·`70.0` 이 두 갈래로 갈렸고 지금은 하나다.


  열 자리인 이유: `residual` 은 도달 가능한 모든 입력에서 `[1, 10)` 이고 그 범위의 ULP 는 `2.2e-16`~`1.8e-15` 다. 열 자리 반올림은 흡수하는 오차보다 **4.75~5.05 자릿수** 위에 있다 — 가장 좁은 곳이 범위 상단이고 `< 7` 분기가 정확히 거기 있다. **갤러리 16개 페이지는 한 바이트도 안 바뀐다**: 전체 빌드에서 `_nice_step` 은 144회 불리고 그중 `magnitude == 0.1` 인 11회의 residual 이 `2.0`·`8.0` 이라 분기 경계 근처에 하나도 없다.

- **수치 컬럼이 문자열을 만나면 어느 컬럼인지 말한다.** `scales.py` 가 이 패키지의 유일한 입력 규칙을 선언한다 — *"마스킹하거나 클리핑하지 않고 **이름을 대고 거부한다**"*. 그런데 **열여섯 차트 중 열다섯**이 맨 `float()` 를 불러 `ValueError: could not convert string to float: 'a'` 를 냈다 — 예외는 `radarplot` 하나뿐이었고 그마저 컬럼이 아니라 카테고리를 댔다. 값이 무엇이었는지는 말하고 **어디서 왔는지는 안 말하는** 문구이고, 40컬럼 프레임을 쥔 사용자는 컬럼을 스스로 찾아야 했다.

  갈라짐은 함수 **안**에도 있었다. `lineplot` 은 x 를 지키면서(`column 'x' holds str, which has no position on an x axis`) y 는 `float()` 로 흘려보냈다 — 같은 차트가 같은 실수에 채널에 따라 두 가지로 답했다.

  `data/_missing.require_number` 하나로 모았다. 값이 아니라 **타입 이름**을 말한다 — 긴 문자열 컬럼이면 사용자 데이터가 예외에 그대로 실리고, 무엇이 잘못됐는지는 종류가 말해 준다(값은 chained cause 에 남는다). `radarplot` 은 카테고리도 함께 대던 것을 잃지 않는다: `context=` 로 덧붙여 `column 'v' holds str, which is not a number (category '다')` 가 된다 — 컬럼은 모든 차트가 갚아야 할 몫이고, 더 말할 수 있는 차트가 그것을 포기할 이유는 없다.

- **`sparkline` 이 자기가 받지도 않는 인자를 지목하던 것을 고친다.** `sparkline(data, y="nope")` 이 `KeyError: "x column not found in data: 'nope'"` 를 냈다. 이 차트에는 `x=` 가 없고 docstring 이 그렇게 적고 있다 — `ingest_longform` 의 첫 필수 채널 이름이 `x` 라서 새어 나온 것이다. 사용자는 자기 코드에 없는 인자를 찾아 헤맸다. 이제 `y column not found in data: 'nope'` 다.

- **`gaugeplot`·`pieplot` 이 `width=` 와 같은 타입 범위를 받는다.** `charts/_layout._finite` 는 `numbers.Real` 로 검사해 numpy dtype 을 받고, 열여섯 차트의 `width=`/`height=` 가 전부 그 경로다. 그런데 `gauge.py` 와 `pie.py` 는 `int | float` 로 검사하는 사본을 따로 갖고 있어서, **같은 호출 안에서** `width=np.float32(400)` 은 통과하고 `vmax=np.float32(100)` 은 거부됐다. `np.float64` 는 `float` 의 서브클래스라 우연히 통과해 좁은 dtype 에서만 갈라짐이 보였다. 사본 둘을 지우고 공용 검사를 쓴다 — `bool` 거부와 유한성 요구는 그대로다.

- **변이 여섯 개가 4,007개를 통과하던 것을 닫는다.** 셋은 **아무것도** 안 잡았고 셋은 갤러리 바이트 비교만 잡았다 — 그 실패 문구는 `stale: ['boxplot.html', 'index.html']` 뿐이라 *무엇이* 바뀌었는지 말하지 않으므로, `git diff` 를 안 읽고 빌드를 다시 돌린 리뷰어는 회귀를 그대로 커밋한다.

  다섯의 공통점은 값이 **문턱이거나 비율**인데 기존 테스트가 전부 그 한쪽에 편하게 앉아 있었다는 것이다. 새 검사는 반대로 짠다 — 현재 값과 변이된 값이 **다른 답**을 내는 입력을 골라, 기록된 문자열이 아니라 산술을 단언한다. `data/semantic.py` 의 `any`(다중 채널에 부분 결측), `charts/_polar.py` 의 `FULL_CIRCLE_TOLERANCE`(`pi + 2e-9`), `scales.py` 의 `_nice_step` 분기점 넷, `stats/box.py` 의 1.5×IQR(fence 안팎 양쪽에 표본 하나씩), `charts/box.py` 의 `_BOX_WIDTH_FRACTION`(그려진 폭 ÷ 밴드 폭).

  여섯 번째(`stats/interpolate.py` 의 `/6`)는 테스트 공백이 아니라 **실제 결함**이었고 #268 에서 고쳤다.

  **쓰다가 두 가지가 스스로 드러났다.** 상자 폭 검사의 첫 판은 `_BOX_WIDTH_FRACTION` 을 import 해 그것과 비교했다 — 상수를 자기 자신과 비교하는 것이라 0.5 로 바꾸면 양변이 함께 움직여 초록불이었다. 리터럴 `0.6` 으로 바꿨다. Tukey fence 도 표본이 한쪽뿐이었다: 울타리 **안**의 값은 1.5 와 1.0 을 가르지만 1.5 와 3.0 은 못 가른다 — 울타리 밖 표본을 따로 넣어야 양쪽이 닫힌다.

  **가드를 쓰다 결함 하나가 더 나왔다.** `_nice_step` 의 `rough / magnitude` 가 이진 부동소수에서 정확하지 않아, 분기점 **위**에 있어야 할 값이 아래로 떨어진다 — `0.7 / 0.1` 은 `6.999999999999999` 이라 `< 7` 분기를 타고 `nice=5` 를 낸다(수학은 10 이라고 한다). 보이는 결과는 **같은 데이터가 단위만 바뀌면 축이 달라지는** 것이다: `(0, 3.5)` 는 눈금 8개, `(0, 35)` 는 4개다. 처음에는 이것을 *"고칠 것은 없다"* 고 적었는데 리뷰가 스윕해 거짓임을 보였다 — `0.015` 는 정확히 나눠지고 `0.07` 은 오히려 **위로** 벗어난다. 관측 하나를 일반 규칙으로 굳힌 것이었다. #273 으로 분리했고, 현재 동작을 **결함으로** 고정해 뒀다 — 고쳐지는 순간 그 핀이 실패하고 그때 지우면 된다.

- **공개 API 표면에서 사용자가 실제로 부딪히던 넷을 고친다.**

  **`Chart.palette()` 를 없앤다.** 인자를 저장만 하고 어떤 렌더 경로도 읽지 않아서, 부르면 출력이 한 바이트도 안 바뀌었다. docstring 둘째 문단은 *"Not wired up"* 이라고 정직하게 적었지만 **첫 줄**은 `Record a palette override` 였고, 자동완성에서 첫 줄만 보는 사용자는 정확히 반대로 이해했다. 배선하는 대신 지운다 — `Chart` 는 완성된 문서이고, 다 그린 것을 다시 칠하는 setter 를 두려면 `theme()`·`title()` 도 함께 와야 앞뒤가 맞는다. 색을 정하는 경로는 `Theme(palette=...)` 이고, 이제 README 에 실행되는 예제로 들어갔다(전에는 한 번도 안 나왔다).

  **`add_caption` 이 `Chart` 를 받는다.** 전에는 `AttributeError: 'Chart' object has no attribute '_resolved_title'` 로 죽었다 — 다른 클래스의 사적 이름이라, 문제도 우회로(`row([chart])` 로 감싸기)도 알려 주지 않았고 그 우회로는 docstring 에도 README 에도 없었다. 이제 한 칸짜리 `row` 로 감싸고 `Composition` 을 돌려준다. `Composition` 을 주면 예전처럼 그 자리에서 변형해 같은 객체를 돌려준다.

  **`spacing` 의 kind 가 세 함수에서 같아진다.** BREAKING. `row`/`column` 은 위치 인자로 받고 `grid` 는 키워드 전용이었다 — `row`/`column` 이 자기 docstring 에서 *"규칙이 사는 곳"* 이라 부르며 값을 그대로 넘기는 바로 그 함수와 달랐다. 키워드 전용 쪽으로 맞춘다: 16개 차트가 데이터 채널 말고는 전부 키워드 전용이고 `test_api_shape.py` 가 그것을 강제한다. `row(charts, 30)` 은 이제 `TypeError` 이고 `row(charts, spacing=30)` 이 맞다.

  **`grid` 의 두 폼이 같은 것을 받는다.** 행렬 폼은 `Composition` 을 늘 받았고(묻지 않고 `chart_size` 를 부른다) span 폼만 거부했다 — "facet 결과를 대시보드 상단에 두 칸 걸쳐 놓기" 라는, span 폼이 존재하는 이유에서 정확히 막혔다. 거부 문구도 이제 둘 다 이름을 댄다(`must be a Chart or Composition`).

  **`grid` 의 두 폼은 열 너비도 다르게 답하고 있었다.** span 폼은 트랙 크기를 `fallback_width` — 그리드 **전체**에서 가장 넓은 셀 — 와 `max` 로 접어서, 그 값이 모든 열의 하한이 됐다. 2400px 차트 옆의 800px 차트가 2400px 자리를 받아 전체가 4812px 이 나왔고, 같은 둘의 행렬 폼은 3212px 이다. 행렬 폼은 처음부터 **교체**한다. 받는 것이 같아졌는데 놓이는 자리가 다르면 "두 폼이 같다" 는 말의 값이 없으므로 함께 고쳤다.

  넷 다 기존 테스트가 하나도 안 보던 것이다 — 전부 고친 뒤에도 4,022개가 그대로 초록불이었다. 그래서 각각에 가드를 넣었고, 열 너비 쪽은 `is not None` 이 아니라 **산술로** 고정했다: 리뷰가 짚은 대로 `is not None` 은 "raise 를 멈췄다" 만 증명하고, 실제로 재 보니 배치는 여전히 틀려 있었다.

- **`interpolate("cubic")` 이 이제 정말 자연 3차 스플라인이다.** `_natural_cubic_spline_coeffs` 는 Burden–Faires 시스템(`alpha = 3 * …`)을 푸는데 그 미지수는 **2계도함수의 절반**(`c_i = S''/2`)이고, `_cubic` 은 Numerical Recipes 형태(`((a³−a)m_i + (b³−b)m_{i+1}) · h²/6`)로 평가하는데 그 식은 **완전한 2계도함수**를 요구한다. 두 공식의 절반씩을 섞어 곡선이 정확히 절반의 곡률만 갖고 있었다 — 함수의 docstring 이 `"Second derivatives at each knot"` 이라고 적은 값이 그 절반이었다.

  결과는 매끄러움이 존재 이유인 방법에서 **매듭점의 1계도함수가 끊기는** 곡선이다. `x=[0,1,2,3], y=[0,1,0,1]` 을 `precision=1601` 로 재면 매듭에서 기울기가 `1.01` 만큼 점프했다(같은 격자에서 `hermite` 는 `0.037`, `lagrange` 는 `0.015` — 순수 유한차분 오차다). 손으로 풀리는 `x=[0,1,2], y=[0,1,0]` 에서 계수는 `[0, -1.5, 0]` 이었고 해석해는 `[0, -3, 0]`, `y(0.5)` 는 `0.59375` 였고 정답은 `0.6875` 다.

  `alpha` 를 `6 *` 로 고쳐 시스템이 진짜 2계도함수를 내게 했다 — `ell[i] = 2*(x[i+1]-x[i-1]) - …` 는 이미 표준 시스템의 계수와 같았다. **`lineplot(interpolate="cubic")` 의 출력이 바뀌므로** 갤러리의 `lineplot` 페이지를 재생성했다(그 페이지의 곡선 둘).

  **왜 4,000개가 못 봤나.** 매듭점에서 `a=1, b=0` 이라 `(a³−a)` 와 `(b³−b)` 가 둘 다 0 이 되어 계수 항 **전체가 소거**된다 — `m` 이 절반이든, 0 이든, 난수든 곡선은 모든 매듭을 지난다. `interpolate` 의 다섯 방법을 보는 테스트는 점 개수와 매듭점 통과, 두 가지 구조적 성질뿐이었고 둘 다 이 소거를 통과한다. 같은 소거가 #262 의 `/6` → `/3` 변이를 무탐지로 만들던 것이고, 그 변이도 이제 빨간불이다.

  새 가드는 세 방향에서 든다: 손으로 푼 해석해와의 대조, 매듭이 **아닌** 다섯 지점에서 독립 구현(Thomas 알고리즘이 아니라 밀집 행렬 + 가우스 소거)과의 대조, 그리고 매듭에서의 1계도함수 연속성 — 마지막 것이 이 결함을 **정의상** 잡는 성질이다. 대조 지점에 `x=1.5` 를 일부러 넣었다: 대칭 픽스처의 대칭점에서는 틀린 답과 옳은 답이 일치하므로, 그 하나만으로는 아무것도 증명하지 못한다는 것을 남겨 둔다.

- **`radarplot` 이 행을 버리면서 그 사실을 말하고, `estimator=` 로 고칠 수 있다.** `barplot` 과 같은 규칙("같은 스포크의 행은 마지막 것이 이긴다")으로 접으면서 `AggregationWarning` 을 내지 않았고, 경고를 못 듣는데 고칠 손잡이도 없었다 — `README.md` 가 이 상태를 이미 적어 두고 방치하고 있었다. 이제 기본 경로가 `barplot` 과 같은 문구로 몇 행이 남았는지 말하고, `estimator=`(`"mean"`/`"median"`/`"sum"`/callable)가 `barplot`·`areaplot`·`lineplot` 과 같은 모양으로 동작한다.

  `charts/_aggregate.py` 는 이 차트를 범위 밖에 둔 이유를 *"접기가 (시리즈, 카테고리) 쌍마다 일어나고 그 다각형에는 구멍이 없어야 하므로, 집계와 구멍 검사가 얽힌다"* 고 적어 두었다. 답은 **얽히지 않는다** 였다 — estimator 는 스포크 위에 선 값들을 접을 뿐 스포크를 만들지도 없애지도 못하므로, 구멍 검사는 접힌 값을 읽고 예전과 똑같은 것을 거부한다. 반지름 검증(음수·비유한)도 접힌 값에 걸리므로 estimator 가 만들어 낸 음수도 거부된다.

  **기존 호출은 바이트 그대로다** — `estimator=None` 이 기본이고 그것이 예전 규칙이다. 접기가 실제로 있는 다섯 가지 호출을 `main` 과 해시로 대조해 확인했다.

- **음수 `corner_radius` 가 더 이상 유효하지 않은 `rx` 를 만들지 않는다.** 같은 규칙("0 보다 클 때만 둥글린다")을 세 차트가 각자 썼고 둘이 다른 질문을 물었다 — `barplot` 은 `> 0`, `histplot`·`boxplot` 은 진리값이었다. `-5.0` 은 truthy 라 뒤의 둘이 `rx="-5"` 를 냈다. 뷰어에 따라 무시되거나 오류가 되는 값이고, 아무 예외도 안 나므로 조용히 잘못된 파일이 나갔다. `nan` 은 셋을 **세 갈래**로 갈랐다: truthy 라 `format_coord` 까지 가서 예외가 난 둘, `> 0` 이 거짓이라 말없이 각진 모서리를 그린 하나.

  `Theme.__post_init__` 이 음수·`inf`·`nan`·`bool`·비수치를 이름을 대고 거부한다 — `opacity` 검증이 거기 있는 것과 같은 이유로, 실패는 렌더 시점이 아니라 그 `Theme` 을 만든 자리에서 나야 한다. 위쪽 한계는 두지 않는다: 사각형 변의 절반을 넘는 반지름은 SVG 자신이 클램프하므로 이 검증이 추측할 최대값이 없다. 렌더 쪽 규칙은 `charts/_layout.corner_radius_attr` 하나로 모았다 — 생성자를 우회해 값이 도달하는 경로에서도 음수는 `rx` 가 되지 않는다.

  가드는 세 차트를 **서로** 비교한다. 차트별로 기대 문자열을 적는 방식이 애초에 이 결함을 살려 둔 방식이었다 — 각 차트의 테스트가 그 차트의 동작을 보고 쓰였으니 셋 다 통과했다. 여기에 "결정이 한 곳에 있다" 를 실제로 실패시키는 검사를 더했다: 헬퍼를 차트 네임스페이스에서 갈아 끼우고 차트가 그 답을 따르는지 본다. 결과만 비교하는 검사로는 이것을 볼 수 없다 — `bar.py` 를 원래 표현으로 되돌려도 오늘은 헬퍼와 의미가 같아서 나머지 전부가 초록불이었다.

- **결측값 판정이 컬럼의 dtype 에 좌우되지 않는다.** `is_missing` 의 타입 검사가 `float` 이었는데, `numpy.float64` 는 `float` 의 서브클래스이고 `numpy.float32`·`float16`·`longdouble` 은 아니다. 같은 숫자를 담은 컬럼이 `float32` 라는 이유만으로 NaN 이 결측으로 안 세어졌다 — `hue` 컬럼에서는 `nan` 이라는 범례 항목이 하나 더 생겼고, 수치 채널에서는 `ValueError: value must be finite, got nan` 이 스케일에서 튀어나왔으며, `radarplot` 은 **다른 예외**를 냈다(`radar values must be finite` vs `the series has no value for '남'`). 판정 기준을 `numbers.Number` 로 넓혔다 — 모든 numpy 스칼라와 `Decimal`·`Fraction` 이 포함되고, `str`·`numpy.str_` 은 비교에 들어가지도 않는다. `numpy.datetime64("NaT")` 는 의도적으로 제외한다(`Number` 가 아니고, 결측 시각은 시간축에 대한 결정을 먼저 필요로 한다).

  `charts/ecdf.py`·`charts/histogram.py`·`charts/area.py` 가 이 판정을 각자 손으로 베껴 갖고 있어서 공유 헬퍼를 고쳐도 안 따라왔다. 셋 다 `is_missing` 을 부르게 했고, 그 과정에서 `areaplot` 이 x 컬럼의 NaN 에서 `ValueError` 로 죽던 것이 `lineplot` 과 같이 그 행을 버리는 것으로 바뀐다 — 두 차트가 같은 데이터에 다르게 답할 이유가 없었다.

  같은 술어가 `numpy.bool_` 을 돌려주던 것도 함께 고쳤다. numpy 스칼라의 `!=` 는 `bool` 이 아니라 `numpy.bool_` 을 답하고, `numpy.float64` 는 `float` 의 서브클래스라 빠른 경로를 탄다 — 그 경로에 변환이 없었다. `numpy.bool_` 은 truthy 이고 `True` 와 같다고 비교되므로 호출부 어디에서도 티가 안 나다가, 런타임 의존성이 없는 패키지의 `data` 모듈이 numpy 타입을 반환한다는 사실이 먼 곳에서(`is True`, `json.dumps`) 드러난다.

  새 가드는 16개 차트 **전부**에 대해 `float` NaN·각 numpy 폭의 NaN·`None` 이 같은 SVG·같은 예외·같은 경고를 내는지 비교한다(`tests/test_data_missing_numpy_parity.py`). 손으로 베낀 판정이 다시 생기면 그 차트에서 걸린다.

- **`radarplot` 이 검은 다각형 위에 그려지던 것을 고친다.** `.grid-line` 규칙에 `fill` 이 없었고 SVG 의 `fill` 초기값은 검정이다. 열다섯 차트는 이 클래스를 `<line>` 에만 붙여서 칠할 면이 없었는데, `radarplot` 은 동심 그리드 링을 그리는 **닫힌 `<path>`** 에 붙인다 — 링이 플롯 전체를 덮었고 공개된 갤러리 페이지도 그 상태였다. 바로 다음 줄의 `.spine` 은 같은 함정을 알고 `fill: none` 을 갖고 있었다. `.tick-line` 에도 함께 넣었다 — 지금은 `<line>` 에만 쓰이지만 같은 사고가 나는 자리다. 갤러리 페이지의 세 예시를 800×600 으로 래스터화해 재면 검은 픽셀이 각각 10.7%·4.1%·24.6% 에서 전부 0.01% 미만으로 떨어진다(남는 것은 눈금 글자의 안티에일리어싱이다). `fill=False` 로 윤곽만 남긴 세 번째가 가장 심한 것이 이 결함의 모양을 말해 준다 — 시리즈 다각형이 링을 가려 주지 않으면 검은 면이 그대로 드러난다.

- **좁히지 않는 `xlim=`/`ylim=` 에는 클립이 안 붙는다.** 클립은 "한계를 줬는가" 가 아니라 "그 한계가 도메인을 좁히는가" 로 결정된다(`chart/_domain.narrows`). 이전 판단은 *"넓히거나 동일한 한계도 뷰포트를 받지만 아무것도 안 자른다"* 고 적어 두었는데 둘 다 거짓이었다 — 경계에 놓인 마크를 그 바깥 반지름만큼 자른다. `facet` 이 바로 그 경로를 밟는다: 축 공유는 패널마다 **합집합**을 넘기는 것이고 합집합은 각 패널의 데이터를 정의상 덮으므로, 한계를 하나도 안 준 호출에서 극단 마커가 반쪽이 됐다. 이제 `facet` 기본 경로의 출력은 클리핑 도입 이전과 바이트 동일하다. 좁히는 창에서 경계 마크가 잘리는 것은 그대로다 — 그건 클리핑의 정의이고 요청받은 동작이다.

- **좁히는 `xlim=`/`ylim=` 이 이제 정말 시야를 자른다.** `apply_limit` 의 docstring 은 처음부터 *"0..300 을 그리는 데이터에 `(0, 100)` 을 주는 호출자는 시야를 자르겠다는 뜻"* 이라고 적었는데, 아무것도 자르지 않았다. 창 밖 값은 창 밖 픽셀로 매핑돼 축 위로, 눈금 위로, 캔버스 밖으로 그려졌다 — `xlim=`/`ylim=` 을 받는 **열 차트 전부**가 그랬다. 800×600 캔버스(플롯 영역 y 30~550)에 `ylim=(15,25)` 를 주면 `violinplot` 의 마크가 -2071~1819, `areaplot` 이 -4910~1330 이었다(`tests/test_charts_clipping.py` 의 픽스처 기준). 헤드리스 브라우저로 재보니 선 하나가 600px 캔버스의 0~599행 전부에 잉크를 남겼다.

  마크를 플롯 영역 크기의 중첩 `<svg>` 안에 넣어 자른다(`charts/_layout.marks_viewport`). `id` 도 `clipPath` 도 CSS 도 쓰지 않는다 — 중첩 뷰포트는 그 자체로 잘리고, 이 패키지가 SVG 안에 `id` 를 쓰는 곳이 아직 없어서 한 페이지에 차트 둘을 놓을 때 그것을 구분해 줄 장치가 필요해지기 때문이다. 축·눈금·범례는 밖에 그대로 있고, **호출자가 한계를 주지 않으면 요소가 아예 안 생긴다** — 그때는 도메인이 데이터에서 나오므로 자를 것이 없고, 기존 출력이 바이트 그대로 남는다. `facet` 은 축을 공유하려고 패널마다 한계를 넘긴다 — 다만 그 한계는 합집합이라 좁히지 않으므로 패널 자체에는 클립이 안 붙는다(위 항목).

### Changed

- **`gaugeplot(value=)` 가 `values=` 가 되고 `labels=` 가 위치 인자가 된다.** BREAKING. `pieplot`·`treemap`·`heatmap` 이 전부 `values=` 인데 `gaugeplot` 만 단수였고, **같은 시그니처 안에서** `labels` 는 복수였다 — 행마다 라벨이 하나인 것은 값이 하나인 것과 같으므로 한쪽만 단수일 이유가 없었다. `labels` 가 `pieplot`/`treemap` 에서는 위치 인자인데 gauge 에서만 키워드 전용이던 것도 함께 맞췄다.

  ```python
  gaugeplot(data, values, labels=None, *, vmin=None, vmax=None, tooltip=False, ...)
  ```

  `gaugeplot(value=…)` 는 이제 `TypeError` 다. `labels=` 를 키워드로 부르던 코드는 그대로 동작한다(위치 인자로 만드는 것은 순수 추가다). **렌더 출력은 바이트 동일하다** — 이름만 바뀐다. 컬럼을 못 찾을 때의 오류 문구도 인자 이름을 따라간다(`values column not found`).

- **`Estimator` 가 export 된다.** `barplot`·`areaplot`·`lineplot` 의 공개 시그니처가 `estimator: Estimator | None` 인데 그 타입이 `charts/_aggregate.py` — 사설 모듈 — 에 있어서, 이 인자에 맞춰 자기 함수를 타입하려는 사용자가 import 할 것이 없었다. `svgplot.charts.__all__` 이 아니라 최상위에서만 내보낸다: 그 목록은 "16개 차트 함수" 라는 뜻이고 테스트 레지스트리 둘이 거기서 자기를 유도한다.

- **`tooltip=` 을 안 받는 여섯 차트가 이유를 적는다.** `areaplot`·`ecdfplot`·`kdeplot`·`lineplot`·`radarplot`·`sparkline` 의 소스에 "tooltip" 이라는 낱말이 **0회** 나와서, 읽는 사람이 결정과 누락을 구분할 수 없었다. 이유는 여섯이 같다 — **시리즈 하나가 마크 하나로 그려지므로** 거기 붙일 `<title>` 이 말할 수 있는 것은 시리즈 이름뿐이다. 범례가 있으면(`hue=` 를 준 경우) 범례가 이미 같은 색으로 그것을 말하고, 없으면 말할 이름 자체가 없다 — `sparkline` 은 `hue=` 를 안 받으므로 언제나 후자다. 기본 호출에서 여섯 중 **넷**은 여기에 `fill: none` 이라 히트 영역이 2px 선 자체라는 문제가 겹친다. `radarplot` 은 `fill=True` 가 기본이라 `fill=False` 일 때만 합류하고, `areaplot` 은 늘 채워져 있어 겨냥할 면적을 갖는다. 동작은 안 바뀐다.

- **키워드 전용 인자 순서가 16종에서 같아진다.** `gaugeplot`·`pieplot`·`regplot` 이 관례에서 벗어나 있었다 — 특히 `regplot` 은 `tooltip=` 이 통계 인자 둘 사이에 끼어 있었다. 순서는 `차트별 옵션 → info → tooltip → width, height, theme → 도메인` 이고 나머지 13종은 이미 그랬다. **키워드 전용 인자는 이름으로만 넘길 수 있으므로 호출 호환성이 100% 유지되고 출력 바이트도 안 바뀐다** — `help()` 와 IDE 자동완성에서 보이는 순서만 달라진다.

  `tests/test_api_shape.py` 가 이 관례를 16종에 대해 실행으로 지킨다. 그중 한 항목은 미용이 아니다: `layout/facet.py` 가 차트 인자를 **이름으로** 찾아 `xlim`/`ylim`/`bins`/`categories` 를 넘기므로, 이름이 어긋난 차트는 오류 없이 축 공유를 잃는다.

- **`boxplot`·`violinplot` 이 `hue=` 없이 그릴 때 카테고리마다 팔레트를 돌리지 않는다.** 이제 셋 다 한 색이다 — `barplot` 은 원래 그랬다. 같은 모양(카테고리 축 + 값 축)의 차트 셋이 서로 다르게 굴고 있었고, 각자 자기 동작을 단언하는 테스트를 갖고 있어서 양쪽 다 초록불이었다. `barplot` 쪽으로 맞춘 이유는 카테고리가 **이미 위치와 축 라벨로 구분되기** 때문이다: 그 아래에서 팔레트가 돌면 색이 나르지 않는 구분을 나른다고 주장하게 된다. 색의 뜻은 이제 `hue=` 하나이고, `hue=` 가 있을 때의 동작은 안 바뀐다(hue 값마다 한 색, 범례가 이름을 붙인다).

  **`hue=` 없이 그린 `boxplot`·`violinplot` 의 출력 바이트가 바뀐다.** `barplot` 과 `hue=` 를 쓰는 차트는 그대로다. `categories=` 로 빈 카테고리를 끼우면 밴드는 여전히 생기지만 팔레트 자리는 차지하지 않는다 — 차지할 자리가 없어졌다. 그 공유가 **hue 로 옮겨가지는 않는다**: `hue_values` 는 패널 자신의 행에서 나오고 `facet` 은 그것을 공유하지 않으므로, 같은 그룹이 패널마다 다른 색이 **될 수 있다** — 한쪽에만 있는 그룹이 공유되는 그룹보다 앞서 정렬될 때 그렇다. `layout/facet.py` 가 열린 한계로 적어 둔 것이고 이번에 그것을 실행하는 테스트를 붙였다.

- **모든 공개 인자가 `Raises:` 밖에서 설명된다.** 커버리지 게이트가 "이름이 docstring 어딘가에 나오는가"를 검사해서, `Raises:` 안에만 나와도 통과했다 — *"if `bins` isn't a recognized spec"* 은 그 인자가 존재한다는 것만 알려주고 무엇을 하는지는 말해주지 않는다. 게이트를 `Raises:` 앞부분으로 좁히고 걸린 15개 함수 27개 인자를 전부 메웠다. `theme=` 는 **16개 차트 전부**가 한 문단을 공유하고, 복사본이 서로 어긋나지 않는다는 것 자체에 테스트가 붙어 있다 — 문단 전체가, 앞 세 줄만이 아니라.

- **클래스도 커버리지 게이트 안에 들어왔다.** `isinstance(obj, type)` 필터 뒤에 `Theme` 의 26개 생성자 인자, `Chart` 의 4개, 공개 메서드·프로퍼티 19개가 검사되지 않은 채 있었다. 메서드의 `Raises:` 는 그 메서드가 **실제로 거부할 수 있을 때만** 요구한다 — 자기 본문의 `raise` 이거나, 이 패키지 함수 한 단계 안의 `raise`. `self.…()` 와 `cls(…)` 호출까지 따라가고, 사설 헬퍼는 한 단계 더 들어간다 — 그 헬퍼의 거부는 메서드 자신의 거부이지 별도의 계약이 아니다. 그 규칙이 문서화되지 않은 `ValueError` 넷을 찾아냈다: `Chart.to_html_table`, `Chart.to_string`, `Composition.to_string`, `Composition.to_markdown`.

- **`Theme` 의 26개 필드에 속성 docstring 이 생겼다.** `theme=` 는 16종 차트 전부의 인자인데 필드 설명은 `#` 주석이라 `help()` 도 IDE 호버도 보여주지 않았다. 그중 **9개는 어느 차트의 출력 바이트도 바꾸지 않는다** — `grid_style`, `tick_direction`, `legend_position`, 그리고 폰트 크기 6개. 각 필드가 스스로 그렇게 적고, 16종을 렌더해서 재는 테스트가 양방향으로 지킨다. `tooltip_font_size` 는 미구현이 아니라 **도달 불가**다: `<title>` 툴팁은 브라우저 크롬이라 이 패키지가 쓰는 어떤 CSS 도 닿지 않는다.

- **`boxplot(mode="tukey")` 는 `"1.5IQR"` 의 동의어가 아니다.** 문서에 그렇게 적으려다 실행해 보고 알았다 — `"tukey"` 는 사분위를 Tukey hinge(각 반쪽의 중앙값)로 잡고 `"1.5IQR"` 은 백분위를 보간한다. 연속분포에서 뽑은 4~12개짜리 표본 20,000개에서 사분위는 **매번** 달랐고 이상치 집합은 여섯 번에 한 번꼴로 달랐다. 동점이 흔한 정수 데이터에서도 사분위는 97% 에서 달랐다. 동작은 그대로고 docstring 이 이제 그것을 말한다.

- **`Theme` 의 죽은 필드 설명이 재는 명제와 같은 문장이 됐다.** "Nothing draws one yet" 같은 말은 테스트가 재는 "출력 바이트를 안 바꾼다"와 **다른 명제**여서, 재는 쪽이 옳아도 적힌 쪽이 틀릴 수 있었다 — 실제로 둘이 틀렸다. `layout.add_caption` 은 캡션을 그리고 `grid(titles=)` 는 제목을 그린다. 다만 각각 14px·13px 로 하드코딩돼 있어서 `caption_font_size`/`title_font_size` 를 안 볼 뿐이다. 이제 아홉 필드가 전부 같은 한 문장을 쓴다.

- **공유 `theme=` 문단에서 "유일한 스타일 입력"이라는 말을 뺐다.** `heatmap` 의 셀 색은 `cmap=` 에서 오고 테마 팔레트는 무시된다 — 13개 중 12개에서 참이고 한 개에서 거짓인 문장이었다. `font_family` 의 "모든 텍스트 요소", `marker_size` 의 "그 범위의 중앙"(중앙은 1.5배다), `corner_radius` 가 건너뛰는 사각형 목록도 같은 이유로 고쳤다.

- **`Chart.palette()` 은 배선돼 있지 않다고 적었다.** 인자를 저장만 하고 아무 렌더 경로도 읽지 않아서, 호출해도 출력 바이트가 안 바뀌고 팔레트가 아닌 문자열도 조용히 받는다. "색 팔레트를 재정의한다" 고 적힌 것을 읽고 차트가 그대로인 것을 본 호출자는 어느 쪽이 고장인지 알 길이 없다. 색을 바꾸는 길은 `theme=` 에 `Theme(palette=…)` 다.

- **`histplot(xlim=)` 이 창 밖 값을 버린다.** 이전에는 가장 가까운 칸으로 접혀서, 데이터보다 좁은 `xlim` 을 준 차트의 **가장자리 막대가 창 밖 값까지 세고 있었다** — `Random(3).gauss(10, 3)` 200개에 `xlim=(8, 12)`, `bins=4` 면 첫 막대가 24개 자리에서 80개, 마지막이 21개 자리에서 63개다(200개 중 98개가 창 밖). 막대 높이와 `<desc>` 의 관측 수가 함께 바뀐다. 좁히는 `xlim` 을 안 쓰는 차트의 출력은 바이트 동일하다. 창이 값을 하나도 안 남기면 이제 `ValueError` 다 — 그리면 값 도메인이 `(0, 0)` 이 되는데 그건 `apply_limit` 이 호출자에게서 거부하는 값이다.

- `png` extra 가 `cssselect2>=0.5` 를 함께 요구한다. cairosvg 는 그것을 통해 래스터화하면서 버전 하한 없이 선언하는데, 0.4.1 은 `:where()` 를 `SelectorError` 로 거부한다 — 위 스코프 변경 이후 모든 차트의 CSS 가 그것을 쓰므로, 오래된 버전이 잡힌 환경에서는 `save("x.png")` 가 되던 것에서 터지는 것으로 바뀐다. 0.5.0 이 `:where()` 를 지원하는 첫 버전임을 릴리스별로 확인했다.

- **차트 CSS 가 문서 단위로 격리된다.** 규칙이 `.series-1 { … }` 대신 `:where(.svgplot-f1a2b3c4) .series-1 { … }` 로 나온다. 인라인 SVG 의 `<style>` 은 SVG 안으로 한정되지 않고 문서 전역이라, 차트 둘을 한 HTML 문서에 넣으면 둘 다 `.series-1` 을 정의하고 나중 것이 앞 것을 덧칠했다 — 이 패키지의 용도가 markdown/HTML 임베드이므로 기본 출력이 그 용도에서 깨지는 것이 버그였다. **요소의 `class=` 는 바뀌지 않는다**: `class="series-1 line-series"` 그대로다. `:where()` 는 특이도를 더하지 않으므로 규칙의 무게가 `(0,1,0)` 으로 유지되고, 호스트 스타일시트의 `.series-1 { stroke: red }` 가 전과 똑같이 동작한다. 접두어만 붙였다면 `(0,2,0)` 이 되어 사용자 재정의가 졌을 것이다. 스코프 클래스는 문서 내용의 sha256 에서 결정적으로 뽑고, `Chart.set_scope(name)`/`Composition.set_scope(name)` 으로 직접 정할 수 있다. `apply_size(…, "responsive")` 의 규칙은 **의도적으로 전역**으로 남는다 — 그 클래스는 루트에 붙으므로 자손 결합자로는 잡히지 않아, 스코프하면 조용히 죽는다. cairosvg 로 래스터화한 PNG 는 스코프 전후 바이트 동일하다.
- `heatmap` 의 출력 크기 추정에 상수항(`_BYTES_FIXED`)이 생겼다. 스코프가 더한 비용은 규칙 수에 비례하는 상수인데 2항 모델에는 그것을 둘 자리가 없어 셀·틱 계수로 흩어졌고, 최선의 2항 재적합조차 5% 한계에 4.90% 로 붙었다. 상수를 이름 붙이니 최악 오차가 2.40% 다.

### Added

**분포·회귀 차트**

- `ecdfplot` — 경험적 누적분포. 정렬과 누적 비율뿐이라 `stats` 모듈을 쓰지 않는다. `stat="count"`, `complementary=`(생존함수) 지원.
- `kdeplot` — 가우시안 커널 밀도. `hue=` 그룹이 **하나의 x 그리드를 공유**해 곡선끼리 직접 비교된다(`histplot`이 bin 경계를 공유하는 것과 같은 이유). `fill=True`는 축까지 채우고 윤곽을 남긴다.
- `violinplot` — 카테고리별 좌우 대칭 밀도. `boxplot`과 **위치 인자 `(data, x, y)`가 같아** 그 부분은 바꿔 쓸 수 있다(키워드는 다르다 — `boxplot`의 `mode=`에 해당하는 것이 없다). 모든 카테고리가 하나의 y 도메인과 하나의 peak을 공유해 폭이 비교 가능하고, `inner="box"`는 기본 모드 `boxplot`이 그렸을 사분위 상자와 일치한다.
- `regplot` — 최소제곱 적합선과 백분위 부트스트랩 신뢰대역. 같은 `seed`면 SVG가 byte-identical하다.

**형태 차트**

- `heatmap` — 값을 **9단계로 양자화**해 색을 고른다. 연속 램프가 아닌 이유는 재테마가 실제로 동작하게 하기 위해서다 — CSS 규칙 9개는 손으로 고칠 수 있지만 셀당 규칙 하나는 못 고친다. 덤으로 범례가 공짜이고(스와치 9개가 기존 `render_legend`로) 출력이 대략 절반이다. `center=`는 발산 컬러맵(`"coolwarm"`/`"purplegreen"`)과 **함께** 줘야 한다 — `cmap=`의 기본값은 sequential이라 `center=`만 주면 거부된다. `annot=True`의 글자 색은 테마가 아니라 **셀 색의 휘도**에서 고르며(셀 색은 컬러맵에서 오므로 모든 프리셋에서 동일하다) 전 프리셋 × 전 컬러맵에서 WCAG AA(4.5:1)를 넘는다. 2,500셀을 넘으면 크기를 경고하되 **막지는 않는다**.
- `radarplot` — 극좌표 위의 선 그래프(pygal 모델). 새 스케일 타입 없이 기존 `CategoricalScale`/`LinearScale`을 각도 스케일로 그대로 쓴다. 눈금 링은 원이 아니라 **다각형**이라 모든 스포크를 그 눈금이 말하는 값에서 지난다.
- `treemap` — squarified(Bruls 2000), 단일 레벨. 면적이 값에 비례한다.
- `sparkline` — 축·범례·라벨 없는 120x24 미니 캔버스. 문장 안에 넣으라고 있는 것이라 800x600 기본 캔버스를 쓰지 않는 유일한 차트다.
- `gaugeplot` — 240도 아크 위의 스칼라 값. 이 패키지의 차트 16종 중 유일하게 데이터 모델이 비교가 아니라 스칼라라 `pieplot`처럼 단일 `value` 컬럼을 받는다. 범위 밖 값은 양 끝으로 **클램핑**된다 — 감아 돌게 두면 큰 값이 더 작은 아크로 그려진다.

**집계**

- `barplot`/`areaplot`/`lineplot`의 `estimator=` — 같은 x 를 가진 여러 행을 어떻게 접을지 고른다. `"mean"`/`"median"`/`"sum"` 또는 그룹의 값을 **행 순서대로** 받는 callable. 기본값 `None`은 각 차트의 기존 규칙을 그대로 둔다(`bar`는 마지막 행이 이기고, `area`는 합산하며, `line`은 두 꼭짓점을 모두 그린다) — 기본값을 seaborn 처럼 `"mean"`으로 바꾸면 이미 만들어 둔 모든 차트가 조용히 다시 그려지기 때문이다.
- `AggregationWarning` — 그 대신 **행이 실제로 버려질 때** 알린다. `barplot`뿐이다: `area`는 합산하고 `line`은 둘 다 그리므로 잃는 것이 없고, 접기만 했다고 경고하는 것은 아무것도 아닌 일에 대한 조언이다. 호출당 한 번만 나온다(범주마다가 아니라).
- 나머지 차트가 `estimator=`를 받지 않는 근거를 `charts/_aggregate.py`에 남겼다 — 분포 차트는 퍼짐 자체가 그림이고, 1행=1마크 차트는 접을 것이 없으며, `heatmap`은 중복 셀을 접는 대신 거부한다. `radarplot`은 `bar`와 같은 규칙을 갖지만 다각형의 "구멍 없음" 검사와 얽혀 있어 이번 범위에서 뺐다는 점도 명시했다.
- `lineplot`에서 `estimator=`와 `info=`는 함께 쓸 수 없다 — 각주 표는 "1행 = 1마크"를 전제하고 집계가 바로 그 전제를 깬다.
- 오차 막대(`errorbar=`)는 이번 범위 밖이며, 넣는다면 밴드나 민선이 아니라 **캡 있는 T자**라는 판단 근거를 모듈 docstring에 남겼다.
**차트 크기**

- 16종 전부가 `width=`/`height=`를 받는다. 15종은 기본값 `None`이 800×600이라 **기존 호출의 출력이 byte-identical**하고 — 16종 전부를 `origin/main`과 대조해 확인했다 — `sparkline`만 `120.0`/`24.0`을 기본값으로 갖는다(그릴 축도 범례도 없어 고를 여백 프리셋이 없다).
- 마진 프리셋이 캔버스에 맞춰 줄어든다. 가로/세로 각각 45%를 넘으면 그 쌍을 같은 비율로 축소한다 — 한쪽만 깎으면 범례 여백과 눈금 라벨 여백 중 하나가 다른 하나를 위해 희생된다. 기본 크기에서는 클램프가 걸리지 않으므로 byte-identical이 유지된다. 이슈의 예시(폭 300)에서 플롯 영역이 80px → 165px가 된다.
- 눈금 개수가 플롯 영역 길이에서 나온다. 기준은 기본 크기가 이미 쓰는 밀도(가로 128px, 세로 104px당 하나)이며, 두 마진 프리셋이 남기는 700px과 580px이 **모두 5로 반올림되는** 값이라 기본 크기에서는 지금과 같은 5개가 나온다. 2~10개로 묶인다.
- **240×180 미만은 거부한다.** 임의의 하한이 아니라 범례 여백에서 푼 **실측 하한 두 개를 넘는** 값이다 — 마진 축소 후 범례 글자에 남는 자리가 `0.32727×폭 − 42`px이고, 축약이 더는 도움이 되지 않는 지점(2.4em)에서 209.0px, 다섯 글자 라벨이 통째로 들어가는 지점에서 227.5px이 나온다. 240 은 둘을 넘으면서 기본값과 같은 4:3(180)을 이룬다. 테스트가 두 하한을 다시 계산하고 240 이 그것을 넘는지 확인한다. 여백보다 긴 라벨은 거부되지 않고 줄임표로 축약되며 전문이 `<title>` 에 남는다.
- 범례 행이 캔버스 아래로 넘치면 **거부한다**(예전에는 캔버스 밖에 그렸다). `heatmap`의 9단계 범례는 상단 여백(y=30) 아래로 **168px** 의 잉크를 놓아 캔버스 높이 **198px** 을 요구한다(실측: 197 거부, 198 통과) — 최소 높이 180 에서는 들어가지 않는다. 가드는 범례가 **점유를 주장하는** 공간이 아니라 실제 **잉크**를 잰다 — 앞의 것으로 재면 들어가는 범례를 거부한다. 실측: `origin/main` 에서 800×600 범례 29개는 마지막 잉크가 y=594 로 정상 렌더되고, 30개가 y=614 로 넘친다. 그 경계 그대로다.

**통계**

- `stats.kde` — 순수 stdlib 가우시안 KDE(Scott/Silverman). `grid_range=`로 여러 표본을 한 그리드에서 평가할 수 있다.
- `stats.regression` — OLS `linear_fit`, 시드 고정 부트스트랩 `confidence_band`. 대역 없이 적합선만 필요한 호출자를 위한 `svgplot.stats.regression.fit_curve`(서브모듈 전용, `stats` 재노출 대상 아님).
- `stats.quantile` — `box.py`의 private 헬퍼를 공개 승격.

**labels와 markdown 출력**

- `Chart`/`Composition`의 `.save("x.md")` / `.to_markdown()` — 인라인 SVG와 각주 표를 한 파일에. `labels/` 패키지가 이것으로 처음 사용자에게 도달한다.
- `lineplot`/`scatterplot`/`pieplot`의 `info=` — 차트가 실제로 그린 행만 표로 병기한다.
- 최상위 `LabelSpec` 재노출.

**접근성**

- **`scatterplot(tooltip=True)`** — 점마다 `<title>` 을 붙인다. 브라우저가 마우스오버 툴팁으로 띄우고, 같은 것이 그 점의 **접근 가능한 이름**이 되므로 이름 없는 점 무더기 하나가 아니라 이름 있는 마크 여러 개가 된다. 내용은 x·y 와 준 경우의 `size=`·`hue=` 이고, 값이 온 컬럼 이름으로 부른다. **기본은 `False`** — 마크마다 `<title>` 은 마크마다 요소라 켜면 기존 출력의 바이트가 바뀐다. SVG 를 페이지에 **인라인**했을 때만 동작한다: `<img>` 안의 SVG 는 별개 문서라 브라우저가 상호작용 없이 그린다. 툴팁에 들어가는 것은 전부 길이가 묶여 있다 — 컬럼 이름은 `<desc>` 와 같은 이름 예산을 넘으면 **이름만 빠지고 값은 남고**, 그룹 라벨은 절이 통째로 빠지며(라벨만으로는 남길 것이 없다), 숫자는 축의 철자와 파이썬의 최단 `repr` 중 짧은 쪽을 쓴다 — 둘 다 정확한 표기라 **반올림하지 않으면서** `1e308` 이 309자리로 읽히는 것을 막는다.

- **`boxplot(tooltip=True)`** — 상자의 여섯 마크(본체·중앙값 선·수염 줄기 둘·캡 둘)에 **같은 문장**을 붙인다. 카테고리, `hue=` 그룹, Q1·중앙값·Q3, 수염 끝, 이상치 수. 반복이 의도다 — 포인터는 자기 밑의 맨 위 요소에서 멈추므로 `<title>` 없는 마크는 글리프에 뚫린 구멍이고, 제목 없는 중앙값 선은 반응하는 상자 한가운데를 가로지르는 죽은 띠가 된다. 이상치 원은 반대로 **자기 값**을 말한다 — 상자는 여러 행의 요약이지만 이상치는 관측 하나다. 수염은 닫힌 구간 `[a, b]` 로 적는다: `1.5IQR` 에서는 양 끝이 실제 관측값이고, `stdev`/`pstdev` 에서는 평균 ± 1 표준편차라 관측값이 아닐 수 있어 `min`/`max` 가 아니라 `whiskers` 라고 부른다.

- **`violinplot(tooltip=True)`** — 바이올린의 세 마크(윤곽·안쪽 상자·중앙값 눈금)에 **같은 문장**을 붙인다. 카테고리, `hue=` 그룹, Q1·중앙값·Q3, 그리고 몇 개의 값에서 나온 곡선인지. `boxplot` 과 같은 이유로 반복한다 — 포인터는 자기 밑의 맨 위 요소에서 멈추므로 상자나 눈금을 제목 없이 두면 반응하는 바이올린 한가운데에 죽은 자리가 생긴다. `inner="box"` 에서는 사분위가 이미 눈금 붙은 축에 그려져 있으니 툴팁이 더하는 것은 정확도이고, `inner=None` 에서는 읽을 길이 아예 없다. 사분위는 안쪽 상자를 그리는 값과 **같은 호출 하나**에서 나온다.

- **`histplot(tooltip=True)`** — 막대마다 `<title>` 에 구간의 양끝과 도수, `hue=` 가 있으면 그룹을 담는다. **어느 행이었는지는 말하지 않는다** — 말할 수 없다. 개수를 세고 나면 그 개수를 만든 값들이 아무 데도 남지 않는다. 구간은 `[12, 18)` 로 적는다: 반열린 구간이고 마지막만 닫히므로 `"12 - 18"` 과 `"18 - 24"` 로 나란히 적으면 `18` 이 어디로 갔는지 두 막대가 서로 다르게 답한다. 경계는 반올림하지 않는다.

- **`heatmap(tooltip=True)`** — 셀마다 `<title>` 에 열·행·**정확한 값**·몇 번째 색조인지를 담는다. 정확한 값에 닿는 유일한 길이다: 색은 9단계로 양자화된 구간이라 한 단계 차이나는 두 셀이 거의 같을 수도 있고 같은 색인 두 셀이 거의 한 단계만큼 다를 수도 있다. `annot=True` 는 셀이 글자를 담을 만큼 클 때만 숫자를 그리고, 범례는 셀이 아니라 단계에 이름을 붙인다. 크기 경고가 이 비용을 함께 센다 — 200x200 빽빽한 격자는 3,352.6KB 에서 6,345.6KB 가 되는데, 그 항이 없으면 경고가 3,385KB(46.7% 낮게)라고 말한다.

- **`treemap(tooltip=True)`** — 타일마다 `<title>` 에 이름·값·비율을 담는다. **라벨이 안 들어간 타일이 이름을 말하는 유일한 길이다**: 40x16px 보다 작으면 라벨을 안 그리는데, 그런 타일이야말로 독자가 찾아봐야 하는 타일이다. 비율은 그림에 없는 사실이 아니라 그림을 읽어주는 것이다 — 이 차트는 값을 면적으로 인코딩한다. 켜면 타일 안 라벨이 `pointer-events: none` 을 받는다: 라벨은 타일 *안*에 있어서 독자가 보는 자리에서 포인터를 가져가고, 그러면 타일의 `<title>` 대신 라벨의 잘림 `<title>` 이 뜬다. 끈 상태에서 규칙을 안 내는 이유는 그 라벨 `<title>` 이 전문을 읽는 유일한 길이어서가 아니라(범례가 언제나 갖고 있다) 타일에 `<title>` 이 아예 없어서, 라벨을 치우는 것이 아무것도 아닌 것과 맞바꾸는 일이 되기 때문이다.

- **`regplot(tooltip=True)`** — 점마다 `<title>` 에 x·y 를 담고, **신뢰대역에도 하나를 준다**. 그 두 번째가 이 차트가 더하는 것이다: 선은 누가 봐도 적합이고 점은 누가 봐도 관측인데, 선을 감싼 반투명 영역은 이름이 붙기 전까지 장식으로 읽힌다 — 차트의 `<desc>` 는 그림 전체에 대해 한 번 말하지, 대역을 가리키는 독자에게 읽히지 않는다. 대역은 몇 % 구간인지와 몇 번의 부트스트랩 재표본에서 나왔는지를 말한다(`n_boot` 은 구현 세부가 아니라 답의 일부다 — 그 수가 이 추정의 정밀도를 정한다). 가리키기 가장 쉬운 마크이기도 하다: 선은 2px 인데 대역은 면적이다. 적합선 자체에는 제목을 안 준다. `ci=None` 이면 대역 툴팁이 없고 `scatter=False` 면 점 툴팁이 없다 — 걸 요소가 없으면 이름을 걸 수 없다.

- **`gaugeplot(tooltip=True)`** — 아크마다 `<title>` 에 이름·값·**그 스윕이 어떤 범위의 몫인지**를 담는다. 범위가 그림이 감추고 있는 것이다: 반쯤 찬 아크는 양 끝을 모르면 아무 뜻이 없고, `vmin` 의 기본값(0, 또는 최솟값이 음수면 그 값)은 보고 알아낼 수 있는 규칙이 아니며, 눈금은 스윕을 표시하지만 양 끝에 라벨이 없다. **그려지지 않는 아크에는 툴팁이 없다** — `vmin` 이하이거나, 양 끝이 같은 좌표로 반올림될 만큼 가까운 값이다. 양끝이 겹친 아크는 렌더러가 통째로 버리므로(SVG F.6.2) 이 차트가 path 자체를 안 내고, 없는 마크에 이름을 붙이면 값이 0이라는 것과 값이 없다는 것이 같은 말이 된다. 범위 밖 값은 스윕이 클램핑되지만 툴팁은 그 행이 든 수를 말한다. 눈금은 대개 낮은 쪽 끝에 라벨을 붙이고(0 이 기본이자 좋은 눈금이라), 빠지는 것은 `vmax` 를 데이터에서 잡았을 때의 높은 쪽이다.

- **`pieplot(tooltip=True)`** — 조각마다 `<title>` 에 이름·값·비율, 그리고 **거기까지의 누적 비율**을 담는다. 마지막 것이 그림 안에 있으면서 어디에도 안 적혀 있는 것이다: 파이는 12시에서 시계방향으로 읽으므로 조각이 *끝나는 자리*가 이미 전체에 대한 진술이고, "이 넷을 합치면 대부분" 은 파이에 가장 자주 묻는 질문인데 눈으로 답하려면 조각을 다시 더해야 한다. 누적은 반올림한 비율이 아니라 값에서 누적한다 — 1/1/1 은 비율이 33.3% 셋이라 더하면 99.9% 지만 마지막 조각의 누적은 100.0% 다. 켜면 값 라벨이 `pointer-events: none` 을 받는다: 라벨은 자기 조각 *위*에 있어서 독자가 겨냥하는 자리에서 포인터를 가져간다.

- **`info=` 가 툴팁도 채운다** — `scatterplot`·`pieplot` 에 `info=` 와 `tooltip=True` 를 함께 주면, 각주 표를 만드는 그 선언 하나가 마크 툴팁의 내용이 된다. 점은 채널 절(x·y·`size=`·`hue=`) 대신 자기 행을 호출자가 적어둔 열 이름 그대로 말하고, 조각은 이름과 값을 그렇게 바꾸되 **차트가 계산한 비율과 누적 비율은 남긴다** — 그 둘은 어느 컬럼에도 없으므로 표가 대신 말해줄 수 없다. 채널 절을 대체하는 이유는 `info=` 가 거의 언제나 차트가 그리는 그 컬럼들을 다시 지목하기 때문이다: 합치면 같은 숫자를 두 번 말한다. **짝은 마크 순번이 아니라 원본 행 번호로 맞춘다** — `hue=` 는 점을 시리즈 순서로 묶고 표는 입력 순서를 지키므로 둘의 순서가 다르고, 행이 빠진 자리에서는 번호마저 어긋난다. 순번으로 세면 모든 점이 이웃의 행을 말하게 되는데 그 행도 호출자의 진짜 데이터라 틀린 티가 나지 않는다. 툴팁 내용은 마크마다 한 벌씩 쓰이므로 길이 상한이 있다. `clause` 가 열 이름만 재는 것보다 엄격한 규칙이다 — `info=` 는 값뿐 아니라 **필드가 몇 개인지**도 호출자가 정하므로 조립된 문장 전체를 잰다. 넘으면 그 마크만 원래 하던 말로 되돌아가고(점은 채널 절, 조각은 이름과 값) 표는 그대로다. 그래서 한 차트 안에서 짧은 행은 `info=` 를, 긴 행은 제 채널을 말하는 일이 생길 수 있다. 스펙이 형식을 못 맞추는 값도 같은 자리로 되돌아간다 — 그 실패는 `to_markdown()` 이 물었을 때 나는 것으로 이미 정해져 있고, 마크마다 스펙을 읽는다고 해서 그리는 시점으로 앞당겨지면 툴팁을 켜는 것만으로 그리던 차트가 아예 만들어지지 않게 된다. 그 과정에서 맨 `@column`(plain)이 정수를 다루는 방식의 결함 둘이 드러나 함께 고쳤다. `math.isfinite` 를 그대로 불러서 `10**400` 같은 큰 정수에 `OverflowError` 를 냈고 — `render_value` 는 `ValueError` 만 낸다고 약속한다 — **2\*\*53 을 넘는 모든 정수를 float 을 거치며 조용히 다른 수로 바꿔 적고 있었다**: `9007199254740993` 이 `…992` 로, `10**23` 이 `99999999999999991611392` 로 나갔다. 호출자의 데이터라고 적힌 각주 표에서다. 이제 정수는 float 을 아예 거치지 않는다 — 넘칠 변환도, 잃을 정밀도도 남지 않는다. float 경로는 그대로다(`30.0` 은 `30` 으로 읽힌다). 2\*\*53 안쪽 정수 2,035개로 두 표기가 한 번도 다르지 않음을 확인했으므로 평범한 데이터의 출력은 안 바뀐다. 숫자 스킴은 큰 정수를 그대로 거부한다. 결측 칸은 표가 쓰는 것과 같은 `—` 로 읽는다. `info=` 만 주면 표만 생긴다: `tooltip=False` 가 여전히 기본이고, 껐을 때의 출력은 예전 그대로다.

- `Chart` 렌더 경로에 접근성 기본값을 연결한다 — `role="img"`/`aria-label`/`<title>`/`<desc>`. 0.1.0에서는 `accessibility.py`가 존재했을 뿐 어떤 렌더 경로에도 연결되어 있지 않아, **차트와 합성 도판 모두** 접근성 요소를 하나도 내보내지 않았다.
- `Composition`(`row`/`column`/`grid`/`facet`) 출력에도 같은 요소를 부여한다.
- 제목을 지정하지 않았거나 공백만 지정한 경우 기본 접근 가능한 이름(`"Chart"`)으로 대체한다 — 보조 기술에 빈 이름을 읽어주는 것보다 낫다.
- **`<desc>`가 차트를 실제로 설명한다 — 16종 전부.** 이전에는 `A chart titled "매출".`이라는 동어반복이어서 스크린 리더 사용자가 얻는 정보가 0이었다. 이제 종류·규모·범위를 말한다: `Bar chart, 3 categories (Mon, Tue, Wed), values 1 to 9.` / `Line chart, 2 series (north, south) over 10 points, x 1 to 5, y 3 to 20.` 문장 조립은 `charts/_describe.py` 한 곳에 있고 차트는 짧은 서술자만 넘긴다. 문구는 영어이며(패키지의 다른 접근성 문구와 같다) 그 안의 이름은 데이터의 언어 그대로라 `lang`은 붙이지 않는다.
- **요약 상한**: 이름은 최대 6개, 합쳐서 60자까지 나열하고 나머지는 `and N more`로 센다. 개수 상한만으로는 한 이름이 200자인 경우를 막지 못하고, 글자 수 상한만으로는 두 글자짜리 이름 열두 개를 막지 못해 **둘 다** 둔다. 이름을 잘라 맞추는 일은 없다 — 잘린 이름은 다른 이름이다. 나열은 항상 앞에서부터의 **접두**이며, 첫 이름조차 한도를 넘으면 괄호 전체를 생략하고 개수만 남긴다. 테스트가 고정한 16종의 문장은 35–102자(중앙값 61.5)이고, 그 수치는 테스트가 표에서 다시 계산하므로 문서와 어긋날 수 없다. 상한이 묶는 것은 **이름 부분**뿐이고(묶음당 60자, `heatmap`만 열·행 두 묶음) 나머지 절은 개수와 값의 **자릿수**로만 자라므로 데이터가 1000배가 돼도 문장은 몇 자 길어질 뿐이다. 값 하나도 상한을 갖는다 — `1e308`의 십진 리터럴은 309자라 이름 상한만으로는 문장이 묶이지 않아서, 16자를 넘으면 지수 표기로 읽는다. 최댓값 하나를 적는 대신 **증가율**을 적는다 — 개수의 자릿수만 자라므로 데이터가 10배가 되면 문장은 약 10자 늘어난다. `heatmap`(16종 중 최악) 실측: 축당 범주 7개에서 239자, 123개에서 254자, 1,234개에서 262자, 12,345개에서 270자. 앞의 셋과 그 사이 증가율을 테스트가 고정한다.
- `<desc>` 문구는 데이터에서 나오므로 `_svg.py` 초크포인트를 거친다 — 개행은 접히고, XML 1.0이 금지하는 문자는 거부되며, 마크업은 이스케이프된다.
- **`info=` 표가 `aria-describedby`로 차트에 연결된다.** 조사 문서가 pygal의 약점으로 지목한 "표는 있으나 접근성 연결 없음"이 그대로 남아 있던 부분이다. `Chart.to_html_table()`이 그 `id`를 단 `<table>`을 내주고, `Chart.set_table_id()`로 한 페이지의 차트마다 다른 `id`를 줄 수 있다(`id`가 겹치면 두 번째 차트가 첫 번째 차트의 데이터로 설명된다). 참조는 **호스트 HTML 문서 안에서만** 성립한다.
- `.md`·Jupyter `_repr_svg_` 출력에는 `aria-describedby`를 붙이지 않는다 — GFM 표는 `id`를 실을 요소가 없고 노트북 셀에는 표가 없어서, 붙이면 허공을 가리키는 참조가 된다. 그 경우 보조 기술은 `<desc>`를 읽는다.
- `labels.render_table(table_id=)` — `format="html"`에서만 유효하고, `format="markdown"`과 함께 주면 조용히 버리는 대신 거부한다.
- `add_caption`이 제목을 따로 지정하지 않은 합성물의 접근 가능한 이름으로 캡션을 채택한다 — 눈에 보이는 캡션이 있는데 보조 기술에는 일반 기본값을 읽어주는 것이 더 나쁘기 때문이다.

**그 외**

- `theme.css`에 `level_colors=`와 `mark_style="outlined"` 추가.
- `palette.Normalize`, `palette.diverging`.
- `CategoricalScale(padding=)` — d3 `scaleBand().padding()`.
- `warnings` — `SvgplotWarning` / `HeatmapSizeWarning`. 경고 정책의 첫 구현이다("출력은 유효하나 품질 저하"면 warn, 무효면 raise).
- `charts/_polar.py` — `pie.py`에서 극좌표 기하 추출. `pie`/`radar`/`gauge`가 공유한다.
- `theme.css`의 `ink_colors=` — 값으로 칠한 마크 **위에 얹는 글자** 색. `level_colors=`와 분리한 이유는 둘이 불투명도에 대해 정반대를 원하기 때문이다: 레벨 색은 다른 마크처럼 `theme.opacity`를 지니고, 잉크는 아래 마크와 대비되도록 고른 색이라 어떤 불투명도든 그 선택을 무효화한다.
- `charts/_series.py` — 7개 차트에 글자 단위로 복제돼 있던 hue 그룹핑 블록을 하나로 모았다. 그 여덟 줄은 서식이 아니라 정책 셋을 담고 있다: **시리즈 순서**(팔레트 색 배정과 범례 순서), **빈 hue 거부**, **hue 없을 때의 표현**. 셋 중 하나를 바꾸면 7곳이 합의해야 했고, 한 곳을 빠뜨리면 차트마다 답이 갈렸다 — `radarplot`에서 실제로 한 번 갈렸다(#104).

### 알려진 제약

의도적으로 범위 밖에 둔 것들이며, 각각의 근거는 괄호 안 위치에 적혀 있다.

- **계층형 treemap 미지원**(`charts/treemap.py` 모듈 docstring) — 단일 레벨만. 계층 입력은 `data/_columns.py`가 표현하지 못하는 트리 구조를 요구한다.
- **`violinplot(split=)` 미지원**(`charts/violin.py` 모듈 docstring) — hue 두 그룹을 한 바이올린의 좌우로 나누는 형태.
- **연속 컬러바 미지원**(`charts/heatmap.py` 모듈 docstring) — `heatmap`이 양자화인 것의 이면이다. 연속 램프는 `<linearGradient>`/`stop-color`가 필요한데, 이는 CSS 클래스 계약 밖의 스타일링이고 합성 시 네임스페이싱 재작성에도 잡히지 않는다.
- **여러 줄 라벨 미지원**(`_svg.py`의 `_fold_newlines` docstring) — 텍스트 노드의 개행은 공백으로 접힌다. 진짜 여러 줄 라벨은 `dy`를 가진 `<tspan>`이 필요하고 이 패키지는 글리프를 측정하지 않는다.
- **markdown 표 셀의 GFM autolink**(`labels/table.py`의 `_escape_markdown_cell` docstring) — 맨 URL·`www.` 접두·이메일 주소는 마크업 없이 링크가 되므로 이스케이프로 막을 수 없다. 값을 고쳐 쓰는 것은 호출자가 주지 않은 데이터를 보고하는 일이라 하지 않는다.

- 캔버스 도입부가 `charts/_layout.new_canvas()` 하나로 모였다 — 15개 차트가 같은 여섯 줄을 반복하고 마진만 달랐다. 함수 단위 중복도 정리했다: `_label_anchor`/`_ANCHOR_EPSILON`은 `charts/_polar.py`(각도 기하가 그 모듈의 자기규정이고, radar·gauge 둘 다 이미 import 한다), `_format_value_label`은 `charts/_layout.format_value_label`(값 라벨은 극좌표 개념이 아니다), `(30.0, 180.0, 30.0, 30.0)`은 `MARGIN_WITH_SIDE_LEGEND`. 16종 차트 출력이 **바이트 단위로 동일**하다(`sparkline` 은 자기 캔버스 크기를 쓰므로 이 헬퍼를 부르지 않는 15종에 들지 않지만, 출력 동일성은 함께 확인했다).

**축 스케일**

- `lineplot`/`scatterplot`의 `xscale=`/`yscale=`, `areaplot`의 `xscale=` — `"linear"`(기본) 또는 `"log"`. pygal(`logarithmic=True`)·matplotlib(`set_yscale("log")`)·Bokeh(`y_axis_type="log"`)가 모두 갖고 있고 이 패키지에는 없었으며, 조사 표(`docs-research/10-feature-matrix.md`)에 **행조차 없어** 의식적 제외도 아니었다. 응답시간·파일 크기·인구처럼 값이 자릿수로 벌어지는 데이터에서 선형축은 작은 값을 한 픽셀에 뭉갠다 — 실측: 1부터 1e6까지 걸친 네 점이 선형축에서는 앞의 두 점이 0.05px 떨어져 한 픽셀에 겹치고 세 번째까지 5.2px 안에 들어오는데, 로그축에서는 세 간격이 모두 173.33px로 균등하다.
- 눈금은 10의 거듭제곱이고, 한 자릿수 안에 갇힌 도메인은 둥근 선두 숫자 사다리(1 / 1,3 / 1,2,5 / 1,2,3,5,7 / 전부)로 나눈다. 지수에 선형 사다리를 쓰지 않는 이유는 그러면 10<sup>0.5</sup> = 3.1622776601683795 가 눈금에 서기 때문이다. 비율이 너무 좁아 둥근 가수로도 최소 눈금 수를 못 채우면(2..3에는 둘뿐) 선형 사다리로 넘어간다 — 그 비율에서 로그축은 시각적으로 거의 선형이다. 19,740개 로그 도메인 스윕에서 최소 눈금 수·순서·중복·도메인 이탈·픽셀 이탈 위반 0건.
- **0·음수·날짜는 거부한다.** matplotlib의 `nonpositive="mask"|"clip"`은 각각 세어 놓은 행을 버리거나 없던 값을 지어낸다. 날짜는 타임스탬프의 로그가 1970을 0으로 골랐다는 사실에서만 나오는 비율이라(2000→2020이 1.6배) 거부한다. 도메인만이 아니라 **그려지는 값도** 검사하므로 `ylim=`이 0을 도메인 밖으로 밀어내도 컬럼명을 대고 거부한다.
- `areaplot`은 `yscale=`을 받지 않는다. 영역 차트의 채워진 면적이 곧 값이고 그것은 0에서 재는데 로그축에는 잴 0이 없다 — 채움이 임의의 자리에서 시작하고 면적이 무엇에도 비례하지 않게 된다. 인자를 열어 두고 호출자에게 데이터 탓을 하기보다 아예 열지 않았다.

**분포 차트의 두 번째 그룹핑 축**

- `boxplot`/`violinplot`의 `hue=` — 카테고리 밴드를 그룹 수만큼 슬롯으로 나눠 나란히(dodge) 그리고 범례를 붙인다. seaborn의 `boxplot(x="day", y="total_bill", hue="smoker")`가 이 인자가 존재하는 이유다. `x=`는 무엇을 나란히 놓을지를, `hue=`는 그 안에서 무엇을 **비교**할지를 정한다 — 다른 질문이고, 후자를 표현할 방법이 없었다.
- 색 규칙이 `hue=`를 따른다. `hue=`가 있으면 hue 값마다 팔레트를 돌아 같은 그룹이 모든 카테고리에서 같은 색이 되고, 없으면 전부 한 색이다. `<desc>`도 시리즈를 읽는다. (처음 넣을 때는 `hue=` 없이 카테고리마다 팔레트를 돌렸다 — 같은 릴리스 안에서 위 항목이 그것을 되돌렸으므로 여기도 결과를 적는다.)
- `hue`는 두 차트에 **동시에** 들어갔다. 한쪽에만 넣으면 위치 인자 형태가 갈려 "`boxplot`과 `violinplot`은 `(data, x, y)`가 같다"는 README의 약속이 조용히 깨진다.

**라벨**

- `info=` 스펙에서 포맷이 **선택**이 됐다. `@field`(중괄호 없음)는 값을 그대로 렌더한다 — 세 스킴(`numeral`/`datetime`/`printf`)이 전부 숫자나 날짜를 요구해서 텍스트 컬럼을 표에 넣을 길이 사실상 없었다(`@name`·`@name{}`·`@name{s}` 모두 거부되고 `@name{%s}`만 통했는데 그것을 가리키는 것이 아무 데도 없었다). 이 문법의 출처인 Bokeh `HoverTool`이 `@field`를 그렇게 다룬다. 숫자는 축과 같은 표기를 쓴다(`2.0`이 아니라 `2`). `@field{}`(빈 중괄호)는 계속 거부한다 — 포맷을 안 쓰기로 한 것과 쓰려다 만 것은 다르다.

### Changed

- **`facet` 마이그레이션 주의.** 축 공유가 켜져 있으면 `plot_fn` 이 시그니처가 받는 만큼의 `xlim=`/`ylim=`/`bins=`/`categories=` 를 **추가로** 받는다. 그래서 (a) `**kwargs` 를 그대로 넘기면서 그중 한 이름을 내부에 고정한 래퍼(`lambda data, **kw: histplot(data, bins=4, **kw)`)는 이제 중복 키워드로 `TypeError` 가 나고 — `bins=4` 를 `facet` 에 넘기거나 `sharex=False, sharey=False` 로 되돌리면 된다 — (b) 그 이름을 **하나도** 받지 않는 `plot_fn` 은 공유가 조용히 꺼진다(거부할 대상이 없다). 래퍼가 필요하면 `**kwargs` 를 받아 그대로 넘길 것.

- **`facet`의 패널이 기본적으로 축을 공유한다**(`sharex=`/`sharey=`로 끌 수 있다). 공유하지 않으면 두 패널의 선이 같은 높이에 그려지는데 하나는 3, 다른 하나는 300이고 그 사실이 지면 어디에도 적히지 않는다 — 축을 확인하지 않는 독자는 조용히 잘못 읽는다. seaborn `FacetGrid`도 둘 다 `True`가 기본이다. 공유는 세 번 렌더하는 비용을 치르며(600행 6패널에서 2.4배 실측 — 눈금이 매겨진 차트의 높이는 x 분할의 결과라, 둘을 한 번에 정하면 높이를 정한 분할을 그 자리에서 갈아엎는다), 합칠 것이 없으면 건너뛴다.
- 직교축 차트 10종이 `xlim=`/`ylim=`를, 카테고리 축 차트 3종이 `categories=`를 받는다. 도메인을 넓히는 것이 아니라 **대체**한다.
- `stats.binning.histogram_bins(bin_range=)` — 값의 극단이 아니라 지정한 범위로 bin 경계를 잡는다. 축만 공유하고 bin 을 공유하지 않으면 막대 폭이 달라지고 "count 3"이 패널마다 다른 양을 뜻한다.
- `histplot` 패널은 bin 의 **범위와 폭을 모두** 공유한다. 범위만으로는 부족한데, `bins="auto"` 같은 전략은 폭을 각 패널 자신의 값에서 구한 뒤 그 폭으로 범위를 덮기 때문이다 — 눈금이 완전히 일치하는 축 아래에서 두 패널이 폭 0.650000 과 0.216667 을 고른다(실측) — 막대 하나가 뜻하는 양이 3배 차이난다. 공유되는 것은 개수가 아니라 **폭**이다: 개수는 그것을 고른 범위를 떠나면 의미가 없어서, 1..3 과 100..102 에 각각 막대 3개인 두 패널이 고른 3 을 1..102 에 다시 펴면 패널마다 통막대 하나가 된다. `bins=`를 정수로 지정하면 그대로 존중한다 — 범위가 공유되면 모든 패널이 같은 구간을 같은 수로 나누므로 경계가 저절로 맞고, 공유할 것이 없다.
- `barplot`/`boxplot`/`violinplot` 의 `categories=` 가 빈 시퀀스·문자열·비문자열 원소를 거부한다. `categories="ab"` 는 이름 하나로 읽히지만 두 개로 풀렸고, 빈 시퀀스는 축과 범례만 있고 마크가 없는 차트를 그렸다.

**아직 공유되지 않는 것**(패널마다 다르게 읽힌다): `hue=` 그룹 색, `heatmap`의 행·열 범주와 값→색 스케일, `radarplot`/`gaugeplot`의 반지름, `kdeplot`의 대역폭(bandwidth) — `histplot`의 bin 과 같은 종류의 문제지만 `kdeplot`은 자신이 고른 대역폭을 보고하지 않는다.
- `bins=0`·음수는 여전히 거부한다(에러 메시지만 바뀐다 — numpy의 `bins must be positive, when an integer` 대신 허용 범위를 말한다). 이 브랜치 중간에 잠시 조용히 1-bin 차트가 됐던 것을 같은 브랜치 안에서 고쳤을 뿐이라, 릴리스 간 동작 변화가 아니다.
- **런타임 의존성이 없다.** `numpy`가 필수 의존성에서 빠졌다 — 쓰는 곳이 `stats/binning.py`의 한 줄뿐이었고, 나머지 통계 모듈은 전부 순수 stdlib이다(`stats/kde.py`가 그 선택의 측정 근거를 담고 있다). markdown 문서에 SVG를 박아넣는 도구가 문서 빌드 파이프라인에 numpy를 끌고 들어갈 이유가 없다.
- 그 한 줄이 하던 일(`numpy.histogram_bin_edges`)을 직접 구현했다. 폴백이 아니라 **대체**다 — 폴백이면 numpy 설치 여부에 따라 같은 입력이 다른 SVG를 내는데, 그건 이 패키지의 정체성("같은 입력 → 같은 SVG")과 어긋난다. 경계는 통상 데이터에서 numpy와 **비트 단위로 동일**하다(9가지 모양 × 9가지 크기 × bin 명세 12종 = 972회 호출, edge 52,064개 비교, 불일치 0). 의도적으로 갈리는 곳이 셋 있다: 상한을 넘는 bin 수를 거부하고(numpy는 만든다) — 다만 상한이 둘이다. 호출자가 쓴 `bins=` 는 `MAX_BINS`(10,000)에서 막고, 전략 자신이 고른 수는 백 배 높은 100만에서 막는다. 스파이크 컬럼의 `fd` 는 1만을 정직하게 넘길 수 있고(패싯 패널이 고른 15,885 는 실제로 그려진다), 그 자리에 호출자용 게이트를 두면 호출자가 쓴 적 없는 숫자를 들어 거부하게 된다, `stone` 전략은 구현하지 않았으며(**breaking** — 쓰던 호출은 남은 7종을 알려주는 `ValueError`가 된다), **값의 크기가 퍼짐을 압도하는 컬럼**에서 `scott`·`doane` 만 bin 이 하나 둘 갈린다. 둘 다 표준편차를 거치는데 그 크기에서는 `값 − 평균`이 마지막 몇 자리만 남기고 상쇄되고, 그 나머지를 이쪽은 정확히·numpy 는 pairwise 로 더해 반올림이 갈린다. 실측: 1e12 이하 0.00%, 1e14 까지 0.05% 미만, 1e15 에서 0.3%, 1e16 에서 1.8%, 최악 두 bin. 표집을 달리한 두 번의 측정이 첫 불일치 지점을 각각 1e13 과 1e14 로 잡았으니 자릿수가 아니라 이 상한을 믿을 것. 초안 둘이 이 문단을 반대 방향으로 틀렸다 — 처음에는 subnormal·float 최대 근처를 지목했으나 실측 0.00% 였고, 다음에는 `math.fsum` 을 지목했으나 CPython 의 `sum` 도 보정을 하므로 둘이 갈리는 입력을 찾지 못했다. 그 초안의 큰 수치들의 진짜 원인은 **분위수 보간**이었다: 가중형이 큰 두 이웃 값의 공통부분을 잃어 사분위 범위가 밀리고 `fd` 가 따라 밀린다 — 되돌려 재면 1e14~1e16 에서 fd bin 수가 3,044 건 중 112 건(3.68%) 어긋나고, 최악은 numpy 의 6,189 막대 자리에 4,642 막대다. 문서로 적는 대신 `stats/quantile.py` 에서 고쳤고(차이가 표현 가능하면 numpy 와 같은 형태를, 표현 불가능하면 오버플로를 피하는 가중형을 쓴다), 그래서 위 수치가 그만큼 작다. 정수 리스트는 numpy 의 정수 dtype 폭 하한(`width < 1 → 1`)까지 재현하며 bool 컬럼도 같다(numpy 가 `uint8` 로 캐스팅하므로). 대조는 `numpy-parity` extra 를 설치하면 재실행된다.
- 부수 효과로 bin 개수가 **설치된 numpy 버전에 의존하지 않는다.** numpy의 `auto`는 `min(fd, sturges)`였다가 **2.3.0**에서 `min(max(fd, sqrt/2), sturges)`로 바뀌었다 — 스파이크가 있는 데이터에서 bin 이 수천 개가 되는 것을 막는 휴리스틱이다. 위임하는 동안에는 독자의 환경이 차트의 막대 수를 정했다.

### Fixed

- **좁은 캔버스에서 카테고리 라벨이 전부 같은 접두사로 줄어 축이 구분을 잃던 문제.** 밴드에 들어가지 않으면 45도로 눕힌다. 카테고리 8개(`2024년 N분기 실적 요약`)를 폭별로 재면 이전에는 폭 600부터 서로 다른 라벨이 **1개**였다 — 축은 그려져 있는데 어느 막대가 어느 분기인지 알 수 없는 상태다. 지금은 폭 300까지 여덟 개가 서로 다르게 남는다. 전문은 `<title>`에 있지만 이 패키지가 놓이는 인쇄물·markdown 정적 렌더에서는 보이지 않으므로 축약만으로는 부족하다.
  - 회전이 푸는 이유는 **예산이 바뀌기 때문**이다. 수평 라벨의 예산은 밴드 폭이라 카테고리가 늘수록 줄어들지만, 눕힌 라벨의 예산은 하단 여백이라 카테고리 수와 무관하다. 조사 문서가 애초에 이 방법을 지목했고(`12-aesthetics.md` §3, `14-scope-recommendation.md`), `10-feature-matrix.md:100`의 **필수** 항목 중 유일하게 구현되지 않은 채 남아 있었다. pygal `x_label_rotation`, matplotlib·seaborn `rotation=`이 같은 것이다.
  - 자동이라 들어가는 라벨은 그대로 수평이고 **기본 크기 출력은 바뀌지 않는다**. 여백은 눈금 길이 + 라벨 간격 + 세로 투영 + 눕힌 줄높이만큼 깊어지고, 왼쪽으로도 뻗으므로 좌측 여백도 함께 넓어진다(좌측을 넓히면 밴드가 좁아져 뻗는 길이가 다시 늘어나는 되먹임은 근사하지 않고 풀었다). 여백 상한이 걸리면 눕힌 라벨도 남은 길이에 맞춰 줄이고 전문을 `<title>`에 남긴다 — 캔버스 밖은 이 패키지가 다른 모든 곳에서 거부하는 것이라 축약보다 나은 선택지가 아니다. 솎아내기 기준도 다르다: 눕힌 라벨은 평행한 띠라서 겹침을 정하는 것은 `pitch × sin θ`이고 길이는 들어가지 않는다.
  - 적용은 `barplot`(세로)·`boxplot`·`violinplot`·`heatmap`. 숫자 축은 대상이 아니다 — 숫자 라벨은 값 자체라 여백을 넓히는 쪽이 맞다.
- `boxplot`이 NaN 카테고리 라벨을 `"nan"`이라는 카테고리로 만들던 문제. `violinplot`은 같은 행을 버렸다 — README가 둘을 바꿔 쓸 수 있다고 말하는데 무엇이 카테고리인지조차 달랐다. 그룹핑을 한 함수로 합치면서 엄격한 쪽(`is_missing`)으로 통일했다.
- 축 눈금 라벨이 캔버스를 넘거나 서로 겹치던 문제. 처방이 라벨의 **성격**에 따라 갈린다. 숫자·시간 눈금 라벨은 그 자체가 값이라 자를 수 없다(`1234567`을 `1234…`로 만들면 축이 거짓말을 하고, `<title>`로도 복구되지 않는다) — 대신 왼쪽 여백을 라벨 폭에 맞춰 넓힌다. 실측으로 `1200000000000000`은 x=-62.8 에서 시작해 `viewBox` 밖에 있었다. 카테고리 라벨은 이름이라 밴드 폭에 맞춰 줄이고 전체 문구를 `<title>`에 남긴다 — 8개 카테고리가 인접 7쌍 전부에서 겹치던 것이 사라진다. 밴드가 라벨 하나도 담지 못할 만큼 좁으면(100×100 히트맵은 카테고리당 5.80px) 줄이는 대신 **솎아낸다** — 격자선과 눈금은 모든 카테고리에 그대로 남고 글자만 줄어든다. 솎아내기는 **양 축 모두**에 적용된다: 왼쪽 축은 라벨이 세로로 쌓이므로 줄 높이(1.2em)를, 아래쪽 축은 가로로 퍼지므로 밴드 폭 또는 가장 넓은 라벨의 폭을 기준으로 한다. 값이 아래쪽 축에 오는 차트(`histplot`·`ecdfplot`·`barplot(orient="h")`)는 자를 수도 여백을 넓힐 수도 없으므로 솎아내기가 유일한 처방이다 — 해상도를 잃을 뿐 값은 훼손하지 않는다.
- 라벨 폭 추정은 테마의 `tick_label_font_size`를 쓴다. 이 값은 공개 필드이고 컨텍스트가 배율을 바꾼다(paper 8px, poster 16px) — 고정값으로 재면 poster에서 3분의 1을 덜 세게 된다.
- 여백 확장은 캔버스의 30%에서 멈춘다. 그보다 넓히면 축을 보이려고 차트를 가리는 셈이다. 짧은 라벨만 있는 차트의 출력은 바이트 단위로 그대로다.
- `heatmap`의 크기 추정식을 다시 적합했다: `drawn*84 + ticks*168`(4개 실측점 최대오차 3.80%). 라벨 회전이 눈금마다 `transform`과 `<title>`을 더하면서 눈금 단가가 163에서 168로 올랐다 — 계수는 그 네 점의 최악 상대오차를 최소화하는 쌍을 전수 탐색으로 골랐다. 양 축 라벨이 솎아지면서 라벨 수가 데이터가 아니라 캔버스로 제한되고(100×100은 눈금 라벨 54개, 200×200은 63개), 긴 라벨이 `<title>`을 함께 싣게 되면서 셀·눈금 단가가 둘 다 움직였다. 유도가 아니라 적합이므로 출력이 바뀔 때마다 다시 맞춘다.

- 긴 범례 라벨이 캔버스 밖으로 나가 사라지던 문제. 기본 캔버스에서 범례에 남는 폭은 118px 이고 이는 CJK 약 10자다 — 한국어 우선 패키지에서 흔한 길이다. 이제 폭을 추정해 줄이고 전체 문구를 `<title>`에 남긴다. 추정은 글리프를 측정하지 않으므로 근사이며, 그 한계와 어느 방향으로 틀리는지는 `charts/_textwidth.py` 모듈 docstring에 적었다. `treemap`의 타일 라벨도 같은 처리를 받는다. 추정이 상한임이 실측으로 확인된 문자는 출력 가능 ASCII·라틴-1 보충·라틴 확장-A(Arial/Helvetica 319자 전수 검증) + 전각뿐이다. 그 밖(키릴·그리스·아랍·히브리·데바나가리·태국어)은 **예산 대비 비율과 무관하게 항상** 전체 문구를 `<title>`에 남긴다 — 임계값으로는 묶을 수 없기 때문이다. `⸻`(U+2E3B)는 폴백 폰트에서 2.493 em 이라 6자가 예산의 3분의 1로 추정되고도 캔버스를 46px 넘어간다. 유럽어(`São Paulo`, `Bénéfice`, `İstanbul`)가 측정 범위에 든 것은 이 규칙의 비용을 줄이기 위해서다 — 그렇지 않았다면 이런 라벨이 전부 `<title>`을 달았다. 그래도 비용은 있다: 측정되지 않은 문자를 쓰는 라벨은 캔버스에 여유가 충분해도 `<title>`을 단다 — 키릴·그리스·아랍·히브리·태국·데바나가리는 글자 수와 무관하게 전부다. 측정된 문자만 쓰는 라벨(한국어·영어·유럽어)은 예산의 60%를 넘을 때만 단다. 두 경로 모두 `tests/test_charts_legend.py`의 코퍼스로 재측정할 수 있다. 그 대신 `main`에서 복구 불가능하게 캔버스를 벗어나던 라벨이 사라진다.
- 추정이 상한임을 보증하는 폰트 범위를 명시한다. `_TITLE_THRESHOLD`는 Arial·Helvetica·Helvetica Neue·SF NS·Verdana·Tahoma·Trebuchet MS·Geneva·Comic Sans MS 아홉 패밀리가 출하하는 모든 웨이트(정적 40개 페이스 + SF NS 가변 인스턴스)의 실측 최악값(1.5560, Helvetica Neue Medium의 `―`)에서 도출했고, **그 밖의 `font_family`를 지정하면 보증이 무효다**(Heiti SC 0.590, Songti SC 0.590, DejaVu Sans Bold 0.621, Ayuthaya 0.508이 필요하다). 이 목록을 좇아 임계를 더 낮추면 거의 모든 라벨이 `<title>`을 달게 되므로 그렇게 하지 않는다.
- `lineplot`이 `datetime.date` 컬럼을 거부하던 문제. `datetime`이 `date`의 서브클래스라 `isinstance(v, datetime)` 검사가 평범한 `date`를 전부 놓쳤고, 컬럼이 수치 경로로 흘러 `float()`에서 죽었다 — CSV·pandas가 가장 흔히 내놓는 타입이다. `date`는 자정으로 승격되며, 한 컬럼에 `date`와 naive `datetime`이 섞여도 승격 후 그린다(무손실이고 실제로 나오는 형태다). 축에 위치가 없는 타입(`time`, 문자열), 날짜에 숫자가 섞인 컬럼, 그리고 `timestamp()`가 처리하지 못하는 `datetime.max` 근처 값은 **컬럼명을 지목하는** `ValueError`로 거부한다.
- **시간축이 로컬 시계의 구멍을 밟지 않는다.** 시계 사다리는 벽시계 `timedelta` 로 눈금을 만드는데, 서머타임이 시작되는 날에는 그 시각이 아예 없다(`America/New_York` 의 2024-03-10 02:00). `TimeScale` 이 `.timestamp()` 로 자리를 잡으면 없는 02:00 이 실재하는 03:00 위로 접혀 **라벨이 다른 두 눈금이 같은 픽셀에 그려졌다** — 라벨 중복을 위치 중복으로 바꾼 셈이라 더 나쁘다. 실측으로 그 하루 288개 도메인 중 뉴욕 15건, 베를린 15건, 런던 11건. 이제 **다른 눈금이 이미 그 순간에 서 있을 때만** 하나를 떨어뜨리고, 둘 중 실재하는 쪽을 남긴다. 무조건 떨어뜨리는 첫 시도는 더 나빴다 — 자정에 전환하는 존(Santiago·Beirut·Havana)에서 일 단위 축이 실재하는 하루를 통째로 잃었고, 사모아의 1950년 30분 점프가 2세기 축에서 **연도 라벨**을 지웠다. 실측으로 그런 헛된 삭제가 진짜 충돌보다 2.4~13.6배 많았다. 가을의 **반복되는** 시각은 실재하므로(없는 게 아니라 두 번 있다) 그대로 둔다. 도메인 양 끝이 같은 순간인 경우(구멍 안에 통째로 들어간 도메인)는 눈금 하나로 답한다 — 그 도메인에서는 어떤 눈금도 같은 픽셀에 놓인다. 자정 전환 존을 포함한 10개 시간대 스윕에서 충돌 0, 헛된 삭제 0, 라벨 중복 0.
- **tz-aware 컬럼이 그린 기계의 시간대로 읽히던 문제.** `lineplot` 이 도메인을 `fromtimestamp` 로 되돌려 tzinfo 를 버렸다 — 같은 UTC 데이터가 UTC 에서 `00:00~12:00`, `Asia/Seoul` 에서 `10:00~20:00`, `America/Santiago` 에서는 포맷까지 달라졌다. `TimeScale` docstring 이 "실행 위치에 무관하길 원하면 aware 값을 넘겨라"라고 안내하는데 이 경로가 그 말을 거짓으로 만들고 있었다. 이제 원본 datetime 으로 도메인을 잡고 눈금 사다리도 tzinfo 를 그대로 들고 간다. **aware 와 naive 를 섞은 컬럼은 컬럼명과 함께 거부한다** — Python 이 둘의 비교를 정의하지 않는데 축은 최소·최대를 구해야 한다. 예전에는 거부하지 않고 `fromtimestamp` 로 **전부 로컬 naive 로 깎아** 다른 질문에 답하고 있었다. `date` 와 **naive** `datetime` 을 섞는 것은 그대로 환영한다 — 자정으로 올리는 것은 무손실이다. 다만 `date` 는 오프셋을 갖지 않아 naive 로 승격되므로, aware `datetime` 과 섞으면 위 규칙에 걸려 거부된다. 오프셋에는 올릴 자리가 없어서, 하나를 골라 붙이는 것은 점을 옮기는 일이 된다.
- **수백 년대 도메인이 눈금 2개로 퇴화하던 문제.** 연 단 사다리만 "count 이하 중 첫 번째"를 쓰고 있었다 — 나머지 두 사다리가 쓰는 "count 에 가장 가까운 것"과 달라, 사다리의 공백(100→250→500→1000)이 절벽이 됐다. 251~275년, 501~725년, 1251~1475년 구간이 전부 눈금 2개였고 이는 이 모듈 자신의 `_MIN_TICKS = 3` 위반이다. "가장 가까운 것"만으로는 부족했다 — 그 선택은 정렬 **이전** 추정치로 하는데 정렬이 양 끝에서 눈금을 하나씩 깎을 수 있어서, 시작 연도가 둥글지 않으면(2001년 기준 72·73년, 715~748년) 여전히 퇴화했다. 월 단 사다리가 처음부터 갖고 있던 `_MIN_TICKS` 가드를 연 단에도 둔다. 시작 연도 1990~2029 × 스팬 1~3000, 12만 도메인에서 위반 0건.
- **하루 안 도메인의 SVG 에는 날짜가 없다**(알려진 한계). 시계 포맷은 눈금이 지닌 연·월·일을 숨기므로 `"14:30"` 은 `"2024-07-04 14:30"` 의 절단이 아니다 — 라벨 진실성 규칙의 유일한 예외다. 축 어딘가에 날짜를 한 번 내보내는 것(matplotlib 의 offset text 같은)은 별도 작업으로 남긴다.
- 시간축 눈금 라벨이 중복되던 문제. 포매터가 `"%Y-%m-%d"`로 고정돼 있어 3시간 도메인의 눈금이 전부 `2024-01-01`이었다(개수는 시작 시각에 따라 5~6개) — 위치는 서로 다른데 이름이 같으니 축이 스스로를 반박한다. 이제 라벨 해상도를 **눈금이 서로 구별되는 가장 거친 포맷**으로 고른다(하루 안이면 `시:분`, 며칠이면 날짜, 몇 달이면 `연-월`, 몇 년이면 연). 눈금 간격 사다리에 3일·4일이 있는데, 없으면 11~13일 도메인(일별 12행)이 2일과 1주 사이의 공백에 빠져 눈금이 **하나만** 그려진다(3·4일 단이 들어가기 직전 커밋 `103e35e` 기준으로 11·12·13일 도메인이 각각 눈금 2개였고, 지금은 1~30일 전 구간에 그런 도메인이 없다). 해상도는 눈금이 아니라 **도메인**을 보고 고르고, 고른 포맷이 **눈금의 정확한 절단**이어야 한다 — 눈금이 하나면 모든 포맷이 그것을 구별하므로 13일짜리 축이 `2024` 하나로 나오고, 도메인 양 끝만 보면 연말을 걸친 36일 축이 `2025`/`2026`으로 나오는데 첫 눈금은 12월 29일에 서 있다. 즉 라벨이 연도에 대해서는 참이고 눈금에 대해서는 거짓이 된다. 간격은 `count`에 가장 가까운 것을 고른다 — "count 이하 중 가장 촘촘한 것"은 사다리의 공백을 절벽으로 만든다(36일 도메인은 주 단위로 5.14개, 2주 단위로 2.57개인데 5.14가 5를 살짝 넘는다고 2주를 택하면 5주짜리 차트에 라벨이 둘 남는다). 고정 간격과 달력 필드 사이의 이음매는 달력 쪽 답이 너무 성기면 고정 간격으로 되돌아가 닫는다 — 경계를 어디에 두든 그 바로 너머는 달 경계를 한두 개만 품기 때문이다. 눈금 위치도 달력 경계에 맞춘다 — 타임스탬프를 그냥 숫자로 보고 "좋은 수"를 고르면 50,000초 같은 간격이 나와 04:13·18:06에 눈금이 서고, 그래서 날짜만으로는 애초에 구별이 불가능했다. 월 단위는 30일 고정이 아니라 달 필드로 넘긴다(30일 고정은 표류해 매달 다른 날에 눈금이 선다). 두 주 이하의 고정 간격은 첫날 자정 기준으로 정렬되므로 이후 달 경계와는 어긋날 수 있다 — 그래서 두 주를 넘으면 달 필드로 넘긴다.
- `add_caption`이 캡션 텍스트를 검증하기 *전에* 캔버스를 키워, 거부된 캡션이 도판을 영구히 늘리고 재시도마다 또 늘리던 문제.
- `lineplot`이 NaN x 하나로 차트 전체를 죽이던 문제(필터가 x의 NaN을 보지 않았다).
- 비ASCII 컬럼명(`@매출{0,0}`)이 라벨 스펙에서 거부되던 문제.
- 표 셀의 개행이 HTML 출력에서 그대로 통과해 markdown 블록을 끊던 문제.
- 표 셀의 markdown 인라인 문법이 그대로 렌더돼 `[click](url)`이 살아있는 링크가, `![x](url)`이 원격 이미지(문서를 여는 사람의 IP 비컨)가 되던 문제.
- 텍스트 노드의 개행이 SVG에 빈 줄을 만들어, markdown에 인라인으로 넣으면 CommonMark HTML 블록이 그 지점에서 끝나고 이후 SVG 원문이 본문으로 파싱되던 문제.

## [0.1.0] - 2026-08-18

첫 릴리스. markdown 문서에 박아넣을 정적 SVG 차트를 만드는 데 필요한 최소 기능 세트를 갖췄다.

### Added

**코어**

- `_svg.py` — SVG 문서 빌더. 패키지 전체의 단일 이스케이프 초크포인트로, 사용자 문자열은 전부 `add_node`/`add_text`/`set_attribute`를 거친다. 태그·속성 이름을 검증하고 `style`/`on*` 속성과 `script` 태그를 차단하며, 좌표는 수식이 아닌 리터럴로 직렬화해 결과물을 손으로 고칠 수 있게 한다.
- `Chart` / `Composition` — 모든 플로팅·레이아웃 함수의 반환 타입. `.save()` / `.to_string()` / `._repr_svg_()`(Jupyter)를 공통 인터페이스로 제공한다.
- 출력: SVG 문자열·파일, Jupyter 인라인 표시, PNG(optional `png` extra, cairosvg 위임).
- `data` — long-form 데이터 인입(pandas DataFrame은 duck-typing으로 지원해 의존성으로 넣지 않음, 컬럼 dict, dict 레코드 리스트)과 `hue=`/`col=`/`row=` 시맨틱 채널 추출, 포인트별 metadata.
- `scales` — linear / categorical / time 스케일과 "nice" tick 생성. 텍스트 폭 측정 없이 동작한다.
- `accessibility` — 차트 루트에 `role="img"` + `aria-label` + `<title>`/`<desc>`를 넣는 기본값.

**테마와 색상**

- `Theme` — ~25키 불변 스타일 스키마. 전역 상태를 바꾸지 않고 렌더 호출마다 명시적으로 전달한다.
- 내장 프리셋 5종(`light` / `dark` / `minimal` / `print` / `high_contrast`), style × context 분리(`paper` / `notebook` / `talk` / `poster`), 시드 컬러 하나로 팔레트를 만드는 `parametric_theme`.
- `palette` — 정성/시퀀셜 팔레트, 미니 언어 파서(`light:#rrggbb`, `blend:#a,#b`, `ch:s=.5,r=-1.5`), 색맹 안전 기본 팔레트(Okabe-Ito), `jet`류 지각적 비균일 컬러맵 차단.
- `theme/css.py` — Theme을 CSS `<style>` 블록으로 렌더링. 임베드되는 모든 값(색상·폰트·클래스명·수치)을 검증해 CSS 규칙 밖으로 새어나가는 것을 막는다.

**통계와 라벨**

- `stats` — 보간 5종(quadratic / cubic / hermite / lagrange / trigonometric), `numpy.histogram_bin_edges`에 위임하는 히스토그램 binning, box plot 통계 5모드(extremes / 1.5IQR / tukey / stdev / pstdev).
- `labels` — Bokeh HoverTool의 필드+포맷 미니 언어를 이식한 `LabelSpec`(numeral / datetime / printf 3스킴)과 각주형 데이터 테이블 렌더러(markdown / HTML).

**차트 타입 7종**

- `lineplot`(시간축·보간), `scatterplot`(hue·size 매핑), `barplot`(수직/수평 × 그룹/누적), `histplot`(자동 binning), `areaplot`(누적 영역), `pieplot`(도넛·값 라벨), `boxplot`(5모드·이상치 마커).

**레이아웃**

- `row` / `column` / `grid` — spacing, `None` 빈 칸, `(chart, row, col, rowspan, colspan)` span 배치. 합성 시 각 자식의 CSS 클래스를 네임스페이싱해 서로의 스타일을 덮어쓰지 않게 한다.
- `add_caption`(공통 캡션), `apply_size`(fixed / responsive), `titles=`로 각 칸에 소제목을 붙이는 탭 대체 관용구.
- `facet` — `col=`/`row=`로 그룹을 나눠 임의의 차트 함수를 반복 호출하고 격자로 배치한다.

### 알려진 제약

- 다이버징 팔레트(`palette.diverging`)는 2차로 미뤘다. 파일과 시그니처만 예약되어 있고 호출하면 `NotImplementedError`가 난다.
- 패널 간 축·색상 공유(`shared_x=` 류)는 레이아웃이 아닌 데이터 레벨 문제로 분리했다. 각 패싯 패널은 자기 그룹 기준으로 독립 스케일링된다.
- `barplot`은 음수 값을 받지 않는다(명시적 `ValueError`).
- v1.0은 JavaScript를 내보내지 않는다. 인터랙션 대신 정적 대체 수단(각주 테이블, 소제목 나열)을 쓴다.

[Unreleased]: https://github.com/lovit/svgplot/compare/2fbe2dc...HEAD
[0.1.0]: https://github.com/lovit/svgplot/commit/2fbe2dc
