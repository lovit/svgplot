TITLE = "barplot"

SUMMARY = "카테고리마다 막대 하나를 그린다. orient= 로 방향을, hue= 와 stacked= 로 그룹을 정한다."

REQUIRES = "x: 카테고리(문자열로 변환된다) · y: 수치(음수 불가) · hue(선택): 아무 타입"

SETUP = """
import svgplot as sp

QUARTERS = {
    "분기": ["1분기", "2분기", "3분기", "4분기", "1분기", "2분기", "3분기", "4분기"],
    "매출": [120.0, 145.0, 98.0, 176.0, 84.0, 92.0, 110.0, 131.0],
    "채널": ["온라인"] * 4 + ["오프라인"] * 4,
}
"""

EXAMPLES = [
    (
        "기본 — 카테고리당 한 행",
        """ONE_ROW = {"분기": ["1분기", "2분기", "3분기", "4분기"], "매출": [120.0, 145.0, 98.0, 176.0]}
sp.barplot(ONE_ROW, x="분기", y="매출")""",
    ),
    ("hue= 는 밴드를 그룹 수만큼 나눠 나란히 놓는다", 'sp.barplot(QUARTERS, x="분기", y="매출", hue="채널")'),
    ("stacked=True 는 같은 자리에 쌓는다", 'sp.barplot(QUARTERS, x="분기", y="매출", hue="채널", stacked=True)'),
    ('orient="h" 는 카테고리를 왼쪽 축으로 보낸다', 'sp.barplot(QUARTERS, x="분기", y="매출", hue="채널", orient="h")'),
    (
        "categories= 는 카테고리 축을 데이터가 아니라 목록이 정하게 한다 — 없는 분기도 자리를 갖는다",
        'sp.barplot(QUARTERS, x="분기", y="매출", hue="채널", categories=("1분기", "2분기", "3분기", "4분기", "5분기"))',
    ),
    (
        "tooltip=True — 막대마다 자기 카테고리와 값을 말한다",
        'sp.barplot(QUARTERS, x="분기", y="매출", hue="채널", stacked=True, tooltip=True)',
    ),
]

INTERACTIONS = {2: "toggle", 3: "toggle"}

NOTES = [
    "다섯 번째의 categories= 는 카테고리 축을 데이터가 아니라 목록이 정하게 한다. 데이터에 없는 5분기도 밴드를 갖고 "
    "막대만 없다 — 여러 차트의 카테고리 축을 맞출 때 쓰고, facet 이 패널마다 이것을 넘겨 축을 공유한다.",
    "두 번째와 세 번째 그림에 체크박스가 붙어 있다. 이 페이지의 CSS 와 마크업이고 JavaScript 는 0줄이다.",
    "세 번째(누적)가 이 갤러리에서 흐리게 하는 쪽을 고른 이유를 가장 잘 보여준다. 한 층을 꺼도 막대의 전체 높이가 그대로 있다 — "
    "감췄다면 남은 층이 아래로 내려앉아 일어나지 않은 재배치를 주장했을 것이다.",
    "tooltip=True 는 막대마다 <title> 을 붙인다. 값은 그 막대 자신의 것이다 — 누적이면 그 층의 값이지 누계가 아니고, "
    "estimator= 가 여러 행을 접었으면 접힌 값이다. 막대의 마크는 집계이므로 뒤에 있는 행들을 말하면 그려지지 않은 것을 말하는 셈이 된다.",
    "음수 값을 거부한다.",
    "같은 카테고리에 행이 여럿이고 estimator= 를 주지 않으면 마지막 행이 이기고, 몇 행을 버렸는지 AggregationWarning 이 알린다. "
    'estimator="mean"/"median"/"sum" 또는 그룹의 값을 행 순서대로 받는 callable 로 접는 방법을 고를 수 있다.',
    "색은 hue= 만 따른다. hue= 가 없으면 카테고리가 몇 개든 막대는 전부 한 시리즈이고 한 색이다 — 카테고리는 이미 위치와 축 라벨로 구분되고, "
    "그 아래에서 팔레트가 돌면 색이 뭔가를 뜻한다고 읽힌다. hue= 가 있으면 hue 값마다 팔레트를 하나씩 가져가므로 같은 그룹이 모든 카테고리에서 같은 색이 된다.",
    'orient="h" 면 값이 x 축으로 가므로 값 범위를 좁히는 인자도 xlim= 이다. xlim=/ylim= 은 화면의 축을 가리킨다.',
    "categories= 로 카테고리 목록을 지정하면 데이터에 없는 카테고리도 목록이 정한 자리에 밴드를 갖는다 — 여러 차트의 카테고리 축을 맞출 때 쓴다. 색은 위 항목대로 hue= 가 정한다.",
]
