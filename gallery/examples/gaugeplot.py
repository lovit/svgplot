TITLE = "gaugeplot"

SUMMARY = "값 하나가 범위 안에서 어디쯤인지를 240도 아크의 채운 길이로 나타낸다."

REQUIRES = "value: 수치(비유한 값 거부) · labels(선택): 아크 이름 · vmin/vmax(선택): 범위"

SETUP = """
import svgplot as sp

KPI = {
    "지표": ["가동률", "적중률", "재구매율"],
    "값": [72.0, 91.0, 38.0],
}
"""

EXAMPLES = [
    (
        "기본 — 행 하나면 범례도 없다",
        """ONE = {"지표": ["가동률"], "값": [72.0]}
sp.gaugeplot(ONE, value="값", labels="지표", vmin=0.0, vmax=100.0)""",
    ),
    (
        "여러 행은 하나의 범위를 공유하는 동심 아크가 된다",
        'sp.gaugeplot(KPI, value="값", labels="지표", vmin=0.0, vmax=100.0)',
    ),
    (
        "vmin=/vmax= 를 주지 않으면 데이터에서 범위를 잡는다",
        'sp.gaugeplot(KPI, value="값", labels="지표")',
    ),
    (
        "tooltip=True — 아크마다 자기 값과 *어떤 범위에 대고* 그려졌는지를 말한다",
        'sp.gaugeplot(KPI, value="값", labels="지표", vmin=0.0, vmax=100.0, tooltip=True)',
    ),
]

INTERACTIONS = {4: "hover"}

NOTES = [
    "네 번째 그림에는 체크박스가 없다. 한 행이 아크 하나라서 토글은 범례가 아니라 데이터 편집이 된다. "
    "대신 커서가 얹힌 아크에 테두리가 생긴다 — 이 페이지의 CSS 이고 JavaScript 는 0줄이다.",
    "tooltip=True 가 아크마다 <title> 에 이름·값·범위를 담는다. 범위가 그림이 감추고 있는 것이다 — 반쯤 찬 "
    "아크는 양 끝을 모르면 아무 뜻이 없고, vmin 의 기본값(0, 또는 최솟값이 음수면 그 값)은 보고 알아낼 수 있는 "
    "규칙이 아니다. 눈금은 스윕을 표시하지만 양 끝에 라벨이 없고, 범례는 스케일이 아니라 아크에 이름을 붙인다.",
    "세 번째 그림과 네 번째 그림은 같은 데이터인데 아크 길이가 다르다. 세 번째는 범위를 데이터에서 잡아 "
    "[0, 91] 이고 네 번째는 [0, 100] 이다. 그림만으로는 어느 쪽인지 알 수 없고, 툴팁이 그걸 말한다.",
    "vmin 이하 값은 아크가 아예 안 그려지므로 툴팁도 없다. 길이 0인 스윕은 양끝이 겹친 아크라 렌더러가 통째로 "
    "버리므로(SVG F.6.2), 이 차트는 그걸 아예 안 낸다. 걸 요소가 없으면 이름을 걸 수 없고, 없는 마크에 이름을 "
    "붙이면 값이 0이라는 것과 값이 없다는 것을 같은 말로 만든다.",
    "tooltip=True 일 때만 가운데 숫자가 pointer-events: none 을 받는다. 숫자는 아크들이 두른 구멍 안, 아크 *위*에 "
    "있어서 차트 한가운데에서 포인터를 가져간다 — 독자가 거기서 찾는 것은 아크의 <title> 이다. 숫자 자신의 "
    "<title> 은 글자가 잘렸을 때만 생기므로, 대개 가운데는 아무 대답도 안 한다.",
    "범위 밖 값은 스윕이 클램핑되지만 툴팁은 그 행이 든 수를 말한다. 클램핑된 수를 말하면 툴팁이 데이터를 "
    "읽어주는 게 아니라 두 번째 그림이 되고, 옆에 적힌 범위가 꽉 찬 아크를 설명해 준다.",
    "한 행이 아크 하나이고 바깥쪽부터 그린다. 모든 아크가 같은 범위를 쓰므로 길이끼리 견줘진다.",
    "범위 밖 값은 양 끝으로 클램핑된다. 감아 돌게 두면 vmax 를 넘은 큰 값이 더 작은 아크로 그려져 데이터를 뒤집어 보이게 된다.",
    "링이 3px 보다 얇아질 만큼 행이 많으면 거부한다. 그보다 얇은 링은 서로 구별되지 않는다.",
    "vmin >= vmax 를 거부하고, 비유한 경계나 넘치는 범위도 거부한다.",
    "16종 중 데이터 모델이 비교가 아니라 스칼라인 유일한 차트다. 그래서 x/y 채널 대신 value 컬럼 하나를 받는다.",
]
